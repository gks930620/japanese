# 화면정의 — 실력 진단 개편 (2026-09-14 · 3차 미응답 필수 2026-09-20)

> ⚠️ **2026-09-20 — 3차(미응답 필수) 반영.** 기획 `진행사항/기획_2026-09_진단개편.md` §21~§30 · 규칙 원본 `설계/05 §15-2` **"막힌 문항 안내 줄"**.
> 바뀐 곳: §1-2(제출 줄 문구 · **막힌 상태 와이어프레임** 신설) · §2(*"미응답 제출"* 행 → **"미응답 시도" · "막힌 뒤"** 행) ·
> §4-2 ⑤·⑥(새 클래스 `.diag-question-help` · `scroll-margin-top`) · §4-3 함정 4~6 · §4-4 마크업의 aria · §5 체크리스트.
> 레벨 고르기·결과·실패 카드·로딩은 **변경 없음.**
>
> **이 문서는 무엇인가**
> `/diagnosis` 개편(**"한 레벨 = 한 판"** + **문항 갈래 묶음**)의 **구현 인계용 산출물**이다.
> frontend-dev가 **이 파일 하나만 보고** 마크업·CSS를 짤 수 있게 와이어프레임·상태 표·CSS 선택자까지 적었다.
>
> ⚠️ **규칙의 출처는 이 문서가 아니다.** *왜* 이렇게 생겼는가 — 왜 알약 스택인가, 왜 통과/미달을 색으로 가르지 않는가,
> 왜 면을 한 겹 더 칠하면 안 되는가, primary를 무엇이 가져가는가 — 는 **`설계/05 §15-2`와 `설계/05 §19`가 원본**이다.
> 여기 있는 것은 **그 규칙을 이번 화면에 적용한 결과**다. 둘이 어긋나면 `설계/05`가 이긴다.
> 동작 규칙(문항 생성·채점·판정)의 원본은 **`설계/09 §3`**, 문구의 원본은 **`진행사항/기획_2026-09_진단개편.md`**다.
>
> **이 문서의 수명**: 구현·QA가 끝나면 **지운다.** 그때부터는 코드가 사실의 출처다.
> 지우기 전에 "다음 화면에도 쓰이는 규칙"이 새로 생겼으면 `설계/05`로 올린다(`.claude/CLAUDE.md` 문서 위치 절).

관련 문서: `설계/05 §15-2`(화면 규칙) · `설계/05 §19`(취향 기준) · `설계/09 §3`(동작) · `진행사항/기획_2026-09_진단개편.md`(문구·인수 조건)

---

## 0. 무엇이 바뀌나 (한 문단)

사용자 요구: *"처음부터 입문⇒N1 가는 게 귀찮다. 입문~N1 중 고를 수 있게 하고, 통과하면 '이 레벨은 충분합니다. 다음 레벨에 도전해보세요' 떴으면 좋겠다."*
→ **계단(입문부터 자동으로 올라감)이 사라진다.** 레벨을 고르고 → 그 레벨 6문항을 풀고 → **통과든 미달이든 결과 화면에서 멈춘다.**
이어서 할지는 결과 화면에서 **사용자가 고른다.** 여러 판을 치면 판정표에 기록이 쌓인다.

함께 들어온 요구: *"테스트별로 문법, 어법, 단어어휘가 잘 나뉘어져 있으면 좋겠어."*
→ 레벨 화면의 6문항을 **갈래(단어·문법·문장)로 묶어** 보여준다. 순서는 이미 고정이었고(09 §3-3), **없던 것은 구분**이다.

**화면은 넷 + 로딩**: 시작(레벨 선택) · 레벨 화면 · 결과(통과/미달) · 실패 카드 (+ 스켈레톤).
넷 다 `.diag-wrap > .quiz-card` **한 장**이다. 판과 판 사이에 중간 화면은 없다.

**3차(2026-09-20)** — 사용자 요구: *"빈칸으로 내면 틀리게 처리됨 — 실수로 건너뛴 걸 수도… 필수체크 해주삼."*
→ **미응답이 있으면 제출되지 않는다.** 막는 방식은 회색 버튼·모달이 아니라 **누르면 첫 미응답 문항으로 데려가는 것**이다(기획 D19·D20).
레벨 화면에 **상태 하나**("제출을 시도했나")가 늘고, 미응답 문항 안에 **안내 한 줄**(`.diag-question-help`)이 생긴다. 화면 수는 그대로 넷이다.

---

## 1. 와이어프레임

### 1-1. 시작 화면 — 어느 레벨부터 볼지 고른다

```
.diag-wrap > .quiz-card

  실력 진단                                         ← .step-caption (3단 라벨)
  어디서 시작할지 알아볼까요                           ← .step-title (1단)

  한 레벨에 6문항, 한 화면에서 한 번에 제출해요.          ┐ 2단 — 읽으라고 있는 글
  통과하면 그다음 레벨에 도전할 수 있어요.               │ 기본 <p> (--t-md · --text)
  어느 레벨인지 모르겠으면 그대로 시작하세요.            ┘  (문구는 기획서를 따른다)

  ┌ .diag-levels (fieldset — 면 없음, 테두리 없음) ─────────┐
  │ 어느 레벨부터 볼까요       ← legend, 3단 라벨            │
  │ ◉ 입문(문자)             ← .quiz-choices > .quiz-choice │
  │ ◯ 왕초보(JLPT N5)           (라벨 + type="radio")       │
  │ ◯ 초급(JLPT N4)     통과  ← 이미 친 레벨만 .diag-level-mark│
  │ ◯ 중급(JLPT N3)                                        │
  │ ◯ 중상급(JLPT N2)                                       │
  │ ◯ 고급(JLPT N1)                                         │
  └────────────────────────────────────────────────┘

  결과는 저장되지 않아요.                             ← .quiz-note (--t-sm · --muted)

  [ 시작하기 ]                                      ← .quiz-result-actions · .k-btn--primary, 화면에 하나
```

- 목록은 `stagePlan`이 준 **입장 가능한 레벨만**(1~6줄, 가변). 준비중 레벨 줄은 **만들지 않는다.**
- 한 줄의 글은 **`{title}({levelLabel})`** — 둘 다 API 값. 코드(`N5`)를 노출하지 않는다.
- **기본 선택 = 목록의 첫 줄.** 아무것도 안 고른 상태가 없으므로 [시작하기]가 `disabled`가 되는 일이 없다.
- 이미 친 레벨만 줄 오른쪽 끝에 판정 낱말(`.diag-level-mark`). 이미 친 레벨도 **다시 고를 수 있다**(잠그지 않는다).

### 1-2. 레벨 화면 — 6문항이 한 화면에, 갈래로 묶여서

```
.diag-wrap > .quiz-card   (640px 중앙, 회색 면 — 카드는 한 장이다)

  초급(JLPT N4) · 6문항                  ← .quiz-progress (--t-sm --muted, tabular-nums)

  단어·어휘                              ← .diag-group-title (--t-md · --w-bold · --text)
  ┌ .diag-question (fieldset — 면 없음, 테두리 없음) ─────┐
  │ 1. 이 단어의 뜻은?        ← legend, 3단 라벨           │
  │ 学生                     ← .quiz-prompt.jp, 1단       │
  │ がくせい                 ← .quiz-prompt-sub.jp        │  ※ kana 줄은 N4·N3에서만.
  │ ◯ 학생                   ← .quiz-choices > .quiz-choice │    N5면 지문이 「がくせい」 한 줄이다
  │ ◯ 선생님                                             │
  │ ◯ 회사원                                             │
  │ ◯ 친구                                               │
  │ ─────────────────  ← 구분선 1px --border (마지막 보기 위) │
  │ ◯ 모르겠어요             ← 언제나 맨 아래              │
  └──────────────────────────────────────────────┘
        ↕ --sp-6 여백만 — **같은 갈래 안에는 선이 없다**
  (2번 · 3번 문항, 같은 모양)

  ────────────────────────────────────  ← **갈래 사이에만** 1px --border (위아래 --sp-8)
  문법
  (4번 · 5번 문항)

  ────────────────────────────────────
  문장
  (6번 문항)

  아직 2문항을 고르지 않았어요 — 모두 고르면 제출할 수 있어요.       ← .quiz-note#diag-remaining, 미응답 0이면 줄 자체가 없음.
                                                                    시도 전에도 보인다(사실) — 시도 뒤에는 조건. 문구는 하나
  [ 제출하고 결과 보기 › ]               ← .quiz-result-actions, 버튼 하나. 언제나 활성(disabled 없음).
                                          aria-describedby="diag-remaining" — 줄이 있을 때만
```

- **제출 버튼 라벨은 하나다** — `제출하고 결과 보기 ›`. *"제출하고 다음 단계로 ›"* 분기는 폐기. *"다 고르고 제출"* 류로도 바꾸지 않는다(라벨은 목적지).
- ⚠️ **2026-09-20 — *"그대로 제출하면 모름으로 처리돼요"* 는 거짓이 됐다.** 이 문구는 화면 어디에도 없다(A51). 위 줄의 급·톤(`.quiz-note`, 경고 띠 아님)은 그대로다.
- **문항 번호는 전체 통번호 1~6.** 갈래마다 1로 되돌리지 않는다.
- **갈래가 0문항이면 제목째 렌더링하지 않는다.** N2·N1은 갈래 2개가 정상이다.
- 갈래 선은 **렌더된 갈래 사이에만** 생긴다(`+` 인접 선택자라 첫 갈래 위에 뜨지 않는다).

### 1-2-b. 레벨 화면 — 미응답인 채로 [제출하고 결과 보기 ›]를 눌렀을 때 *(2026-09-20 신규 상태 — 2번·4번이 미응답인 예)*

> 규칙 원본은 05 §15-2 **"막힌 문항 안내 줄"**. 문구는 기획 D22 확정본. 여기는 **어디에 무엇이 놓이는가**만.

```
.diag-wrap > .quiz-card

  초급(JLPT N4) · 6문항

  단어·어휘
  1. 이 단어의 뜻은?                     ← 답함 — 안내 줄 없음. 블록은 평소와 완전히 같다
  学生 / がくせい
  ◯ 선생님  ◉ 학생  ◯ 회사원  ◯ 친구  /  ◯ 모르겠어요

  ┌ .diag-question  role="radiogroup" aria-invalid="true" aria-describedby="diag-q2-help" ┐
  │                                        ← 화면이 **여기로** 온다: 블록 위가 GNB 아래 --sp-4 여백에 닿게(scroll-margin-top)
  │ 2. 이 단어의 뜻은?                     ← legend (그대로: --t-sm · --w-bold · --muted)
  │ 답을 골라 주세요 — 모르면 [모르겠어요]를 고르면 돼요.   ← .diag-question-help#diag-q2-help — --t-sm · --err-text · 굵기 없음
  │ 先生                                   ← .quiz-prompt.jp (그대로)
  │ せんせい
  │ ◯ 학생                                 ← **초점 여기**(첫 라디오, focus({preventScroll:true})) — 화살표로 바로 고른다
  │ ◯ 선생님  ◯ 회사원  ◯ 친구
  │ ───────
  │ ◯ 모르겠어요
  └──────────────────────────────────────────────────────────────────┘
  3. …(답함 — 표시 없음)

  ────────────────────────────────────
  문법
  4. 이 문법의 뜻은?
     답을 골라 주세요 — 모르면 [모르겠어요]를 고르면 돼요.   ← **미응답 전부**에 같은 줄 (첫 문항만이 아니다 — Q13)
     …
  5. … 6. …(답함)

  아직 2문항을 고르지 않았어요 — 모두 고르면 제출할 수 있어요.   ← 그대로 그 자리. 화면이 위로 가서 안 보이는 것이 정상
  [ 제출하고 결과 보기 › ]                                        ← 여전히 활성. 잠금·언마운트 없음
```

- **문항 블록에서 바뀌는 것은 안내 줄 하나뿐이다.** 면·테두리·번호 색·아이콘·보기·여백 전부 그대로(기획 §24-2 ②). 답한 문항은 평소 그대로다.
- **자리는 legend 바로 아래, 지문 위**(fieldset의 두 번째 자식). 이동·초점이 도착하는 곳이 블록 위이고, "어떻게"(안내)가 "무엇"(지문·보기) 앞에 와야 한다.
  보기 아래에 두면 [모르겠어요] 구분선 뒤에 붙어 그 보기의 각주로 읽힌다 — 05 §15-2.
- **시도 전에는 DOM에 없다**(A50). 답을 고르면 **그 문항의 줄만 즉시** 사라진다. 새로 붙는 일은 없다(라디오는 되돌릴 수 없다 — D21).
- **이동은 즉시(instant)다.** `scrollIntoView({ block: "start" })` — `behavior: "smooth"`를 넘기지 않는다(§4-3 함정 5). 이미 보이는 자리면 초점만(E31).
- 남은 수 줄은 **시도 전과 같은 자리·같은 문구**다 — 시도 뒤에 톤을 바꾸거나 경고 띠로 올리지 않는다(D22).
- 3~5문항 판도 같다 — 만들어진 문항 전부에 답해야 한다(E29). `{n}`은 그 판의 수.

### 1-3. 결과 화면 — 한 화면, 두 변형

```
.diag-wrap > .quiz-card        ── 통과 ──────────────────────────

  진단 결과                                        ← .step-caption
  이 레벨은 충분해요                                 ← .diag-headline — 화면에서 가장 큰 글자
  중상급(JLPT N2) 문항을 5 / 6 맞혔어요.               ← 본문 한 줄 (판이 1개일 때 = 표 대신)

  [ 다음 레벨 도전하기 › ]  [ 중상급(JLPT N2) 코스로 시작하기 ]  [ 다시 진단하기 ]
  이 결과는 저장되지 않아요. 새로고침하면 사라져요.    ← .quiz-note


.diag-wrap > .quiz-card        ── 미달 ──────────────────────────

  진단 결과                                        ← .step-caption
  여기는 아직 새로운 게 많아요                        ← .diag-headline
  중상급(JLPT N2) 문항을 2 / 6 맞혔어요.

  ┌ 레벨 │ 정답 수 / 문항 수 │ 판정 ┐             ← 두 판째부터. 친 판만 행, 레벨 순
  │ 중급(JLPT N3)  │  5 / 6  │ 통과 │
  │ 중상급(JLPT N2)│  2 / 6  │ 미달 │
  └───────────────────────────┘

  [ 초급(JLPT N4) 코스 시작하기 › ]  [ 한 단계 아래로 해보기 ]  [ 다시 진단하기 ]
  이 결과는 저장되지 않아요. 새로고침하면 사라져요.
```

**버튼 배치** — 어느 변형에서도 **primary는 하나**다(근거는 05 §15-2).

| 변형 | primary | secondary | ghost |
|---|---|---|---|
| **통과** | **[다음 레벨 도전하기 ›]** | [{이 레벨} 코스로 시작하기] | [다시 진단하기] |
| **미달** | **[{추천 코스} 시작하기 ›]** | [한 단계 아래로 해보기] | [다시 진단하기] |

- 순서는 primary → secondary → ghost 고정. ≤768px에서 셋이 세로 100%로 쌓인다(`.quiz-result-actions` 기존 규칙).
- **버튼이 하나 빠지면 다음 순위가 primary를 물려받는다.** 버튼이 둘이 되는 것이지 primary가 둘이 되지 않는다.
- ~~**판이 1개면 판정표를 그리지 않는다.** 두 판째부터 표가 생기고 **그 순간 위의 본문 한 줄은 사라진다.**~~
  → ⚠️ **폐기(2026-09-15 senior-dev 판정)**: **행이 하나여도 표를 그린다**(기획 D12 · A32 · 09 §3-7). 기획이 확정한 결과 문구(§14-3)에는
  *"n / 총 맞혔어요"* 줄이 없어서, 첫 판에서 표를 빼면 **정답 수가 화면 어디에도 남지 않는다**(A19의 네 가지 사실 위반).
  위 와이어프레임의 "본문 한 줄 (판이 1개일 때 = 표 대신)"과 "두 판째부터" 주석도 같은 이유로 낡았다.
- 표는 **레벨 순(낮은 것 → 높은 것)**, 같은 레벨 재도전은 행을 늘리지 않고 **최근 결과로 덮는다.**
- 판정 열은 **낱말**이다(통과/미달 둘 다). 색·배지를 쓰지 않는다. 최종 낱말은 기획이 정한다(§6).

### 1-4. 실패 카드 (재료 부족 · 호출 실패 공용)

```
.diag-wrap > .quiz-card

  실력 진단                                        ← .step-caption
  문제를 준비하지 못했어요.

  [ 일단 {첫 코스}부터 시작하기 › ]  [ 다른 레벨 고르기 ]  [ 다시 시도 ]
```

- `--err` 면·아이콘을 **깔지 않는다** — 사용자가 뭘 잘못한 게 아니다.
- primary는 **언제나 학습으로 나가는 버튼**이다(다시 눌러도 같은 곳에 머무를 수 있는 버튼을 주 버튼으로 두지 않는다).
- `[다른 레벨 고르기]`가 `[다시 시도]`보다 위인 이유: **재료 사정이 레벨마다 다르므로** 성공 확률이 높다.
- **판정표를 얹지 않는다.** 기록은 사라지지 않는다 — `[다른 레벨 고르기]`로 돌아가면 시작 화면 목록의 판정 표시가 그대로 있다.

---

## 2. 상태별 UI — 빠뜨리면 안 되는 것들

| 상황 | 화면 |
|---|---|
| **코스 목록 로딩 중** | `.diag-wrap` 카드 한 장 + `.k-skeleton` 줄 2개, `<section aria-hidden="true">`. 현행 그대로 |
| **코스 목록을 못 부름** | 진단 화면을 그리지 않고 기성 `ApiErrorCard`(05 §8) — 레벨 목록을 만들 재료가 없다 |
| **입장 가능한 레벨이 0개** | 시작 화면을 그리지 않고 **실패 카드**. 코스가 하나도 없으면 그 카드에 **[다시 시도]만** 남는다(primary가 될 코스가 없다) |
| **문항 준비 중**(레벨을 고르고 [시작하기]를 누른 뒤) | 카드 한 장 + `.k-skeleton` 줄 3개(`sk-line w40` · `sk-line w70` · `sk-row`), `aria-hidden`. **시작 화면 자리를 그대로 갈아끼운다** — 화면 전환이라 05 §8의 "갱신 로딩은 흐리게"가 아니라 스켈레톤이다 |
| 고른 레벨이 **3~5문항**만 만들어짐 | **평소와 똑같은 레벨 화면.** 문항이 그 수만큼 서고 진행 줄이 *"· 4문항"* 이라 말한다. **사과 문구·경고 띠 없음** — 사용자가 모르는 사정을 굳이 알리면 제품이 고장 난 것처럼 보인다. 갈래가 비면 그 갈래는 제목째 없다 |
| 고른 레벨이 **3문항 미만**(= 문제 준비 실패) | **실패 카드**(§1-4) |
| **호출 실패**(네트워크·5xx) | **같은 실패 카드.** 문구도 같다 |
| **통과 + 더 올라갈 레벨이 없음**(가장 높은 레벨 통과) | 통과 변형에서 **[다음 레벨 도전하기]가 미렌더**되고 **[이 코스로 시작하기]가 primary를 물려받는다.** 버튼 2개(primary + ghost). 준비중 코스가 **실제로 있을 때만** *"그 위 코스는 지금 준비하고 있어요"*(`.muted-text`) 한 줄. 축하 연출·오렌지 배지를 새로 만들지 않는다 |
| **미달 + 더 내려갈 레벨이 없음**(가장 낮은 레벨 미달) | 미달 변형에서 **[한 단계 아래로 해보기]가 미렌더**. 버튼 2개(primary [첫 코스 시작하기] + ghost). 머리글은 *"여기부터 시작하면 돼요"* 쪽 톤 — **가장 낮은 레벨 미달은 진단의 정상적인 결론이다**(문구는 기획) |
| **미응답인 채로 제출 시도**(1개 이상 — 2026-09-20) | **제출되지 않는다** — 결과 화면·판 기록·채점·추천 어느 것도 없다(A48). 화면이 **번호가 가장 작은 미응답 문항**으로 이동(스크롤 + 초점. 이미 보이는 자리면 초점만 — E31)하고, **미응답 문항 전부**에 `.diag-question-help` 한 줄(§1-2-b). 남은 수 줄은 그 자리 그대로. **확인 모달·경고 띠·토스트 없음, 버튼 `disabled` 없음** |
| **막힌 뒤** | 보기·버튼이 **살아 있다**(잠금·언마운트·가드 없음 — D23·A53). 답을 고르면 **그 문항의 줄만 즉시** 사라진다. 일부만 채우고 다시 누르면 **남은 것 중 첫 미응답**으로(E28). 연타는 매번 같은 일, 부작용 없음(E27). 다 채우고 **다시 누르면** 평소처럼 제출 — 자동 제출 없음(A54) |
| **한 문항도 고르지 않고 시도**(E26) | 1번 문항으로 이동·초점, **만들어진 문항 전부**에 줄. 남은 수 줄은 *"아직 6문항을…"*. 줄 여섯이 **한 화면에 다 보이지 않는다**(문항 한 장 ≈ 300px, 한 화면에 두 장 남짓) — 고르는 대로 사라진다 |
| **전부 [모르겠어요]로 제출** | [모르겠어요]는 **답**이다(D24). 미응답 0 → **제출된다** → 0/6 미달 결과 화면(채점은 오답 그대로). 옛 *"모든 문항 미응답으로 제출 → 미달 화면"* 행은 **이 경우로만** 남는다 |
| **막힌 뒤 새로고침 / 뒤로가기** | E19 그대로 — 레벨 고르기 화면, 기록 없음. 막힌 판은 애초에 기록이 없다(E30) |
| **빈 갈래**(N2·N1의 문장 / 재료 부족) | 그 갈래 블록을 **통째로 미렌더.** 제목만 덩그러니 남기지 않는다. "0문항"·"이 레벨에는 문장 문항이 없어요"를 만들지 않는다 |
| **갈래가 1개만 남음** | 그래도 **제목은 그린다.** 제목은 구분 장치가 아니라 이름표이고, 화면 구조가 레벨마다 달라지지 않는 편이 낫다 |

**한 판 모델에서 사라진 분기 (⚠️ 구현 시 주의)**

- **"첫 단계 / 2단계 이후" 구분이 없다.** 판마다 독립이므로 **문항을 못 만들면 언제나 실패 카드**다.
  앞 판의 기록이 있어도 결과 화면으로 넘기지 않는다 — 이 판은 판정이 없다.
- 폐기 문구: *"{레벨} 단계는 문제를 준비하지 못해 여기까지로 판단했어요."*
- **제출 → 결과 사이에 로딩이 없다.** 채점이 로컬이고 다음 문항을 부르지 않는다.
- **[다시 진단하기]는 시작 화면으로 돌아가되 판정표 기록은 유지한다.** 기록을 지우는 길은 새로고침뿐이고,
  그 사실은 *"이 결과는 저장되지 않아요"* 한 줄이 이미 말한다 — 초기화 버튼을 새로 만들지 않는다.
- 모든 갈래에서 **상단 안내 띠는 한 번에 하나**다.

---

## 3. 탈락 시안 — 다시 제안하지 말 것

### 3-1. 레벨 선택의 형태

| 탈락 형태 | 왜 안 되나 |
|---|---|
| 셀렉트(`.k-select`) | 6개가 **접혀 있어** 무엇을 고를 수 있는지 안 보인다. 열고 → 스크롤하고 → 고르는 3동작이라 **고르는 수고가 늘어난다** |
| 2열 카드 그리드 | 읽는 순서가 Z자로 꺾여 **"낮은 것 → 높은 것"이라는 유일한 정보가 사라진다.** 줄도 어긋난다(05 §19-5) |
| 6개 버튼(누르는 즉시 시작) | 진단은 한 주소 안의 상태 전환이라 레벨 화면에서 **뒤로 갈 곳이 없다** — 오조작을 새로고침으로만 되돌리게 된다. 또 05 §3-2(화면당 primary 1개)를 깬다 |
| 레벨 목록을 회색 면으로 묶기 | 면 계층이 이미 셋이라 **흰 알약이 묻힌다**(05 §19-7). 근거는 아래 3-3 |

**채택**: 세로 알약 스택(`.quiz-choices`/`.quiz-choice` + 라디오) + 기본 선택 + [시작하기] primary.
비용은 탭 두 번이고, 이 개편이 없애려는 수고는 *"30문항을 먼저 푸는 것"* 이지 탭 한 번이 아니다.

### 3-2. 갈래 구분의 수단

| 탈락 | 왜 안 되나 |
|---|---|
| 문항 선 **유지** + 갈래 선 **추가** | 같은 굵기 1px 선이 두 겹이라 **묶음이 안 읽힌다.** 한 화면에 선이 7개가 된다 |
| 갈래 선만 `--border-strong`으로 굵게 | 640px 한 단에 굵기가 다른 선이 섞이면 **표처럼 보인다**(05 §13 "고밀도 관리자 표" 회피) |
| 선 없이 여백 차이만(`--sp-6` vs `--sp-10`) | 스크롤하며 읽을 때 경계가 흐려진다. 선은 "있다/없다"라 **굵기 차보다 강한 신호**다 |
| 갈래마다 아이콘·색·번호 원판 | 05 §19-4 — 강조 수단은 하나. 코스 목록에서 이미 뺀 장식이다 |

**채택**: 구분 장치를 **더하지 않고 옮겼다.** 같은 갈래 안 문항 사이 = 여백만, 갈래 사이 = 선 + 큰 여백 + 제목.
선이 5개 → 2개로 줄고, 남은 선의 뜻은 하나다: *"여기서 갈래가 바뀐다."*

### 3-3. 갈래·레벨 목록을 면으로 묶기 (⚠️ 가장 하고 싶어지는 일)

면 계층이 이미 **세 겹**이다 — 페이지 `--bg`(흰) → 카드 `--card-bg`=`--surface-alt`(회색) → 보기 알약 `--surface-inset`(카드 안에서 `--surface` = 흰).
네 번째 면은 **카드와 알약 사이**에 끼어야 하는데:

- 쓸 수 있는 값이 `--surface-hover`(#eef0f9)·`--bg-sunken`(#f2f3fb)뿐이고, 둘 다 `--surface-alt`(#f5f6fa)와 **눈으로 구별되지 않으면서**
  그 위에 얹힌 **흰 알약의 대비만 깎는다.** 얻는 것 없이 잃기만 한다(05 §19-7 조용한 실종).
- `--point-soft`는 **"고른 것"이라는 뜻을 이미 갖고 있다**(05 §19-3 — 면 색은 역할당 하나씩).
- 결정적으로 `--surface-inset`은 **상속으로 계산되는 토큰**이다(05 §19-7-1). 중간에 면을 끼우면 그 안 알약 **30개(6×5)** 를 직접 다시 칠해야 하고,
  그 순간 면 계층이 구조에서 절차로 되돌아간다.

### 3-4. 결과 화면의 통과/미달 표현

| 탈락 | 왜 안 되나 |
|---|---|
| 통과 `--ok` / 미달 `--err`·`--warn` 글자색·면 | ① 05 §2 — 의미색은 **상태 표시용**이고 여기는 상태가 아니라 **다음 행동의 근거**다 ② 통과를 초록으로 칠하는 순간 미달이 자동으로 빨강의 자리를 갖는다 — **진단이지 시험이 아니다** ③ `.diag-headline`은 §19-2의 1단이고 1단의 색은 `--text`다 |
| 통과 시 폭죽·체크마크·일러스트 | 진단은 머무는 곳이 아니라 지나가는 문이다. 연출은 방을 만든다 |
| 갈래별 정답 수·정답률 열 | 2~3문항짜리 갈래 점수는 사실상 **문항별 정오 노출**이고(09 §3-1 위반), 판정에 쓰지 않는 숫자를 화면에 두지 않는다(05 §19-6) |
| ~~판 1개인데 1행 판정표~~ | ~~머리글이 이미 레벨·판정을 말했다 — 같은 말 두 번(05 §19-6)~~ → ⚠️ **뒤집힘(2026-09-15)**: 머리글은 판정만 말하고 **정답 수는 표에만 있다**(확정 문구 §14-3). 표를 빼면 같은 말 두 번이 아니라 **한 번도 안 하는 것**이 된다 → A32대로 한 행도 그린다 |

---

## 4. CSS — 이미 있는 것으로 되는 것 / 새로 필요한 것

구현 대상: `frontend/src/styles.css` **맨 아래 진단 블록**.

> ⚠️ **새 디자인 토큰은 0개다.** 아래에 나오는 이름은 전부 `design_kits_lets/tokens.css`(또는 `frontend/src/styles/project-tokens.css`)에
> **실재하는 값**임을 파일에서 직접 확인했다:
> `--border` `--bw` `--sp-2` `--sp-3` `--sp-5` `--sp-6` `--sp-8` `--t-sm` `--t-md` `--w-bold` `--text` `--muted` `--point` `--point-soft` `--surface-inset` `--sh-point`
> · **3차(2026-09-20) 추가 확인**: `--err-text`(tokens.css 90행 · 다크 143행) `--nav-h`(30행, 62px) `--sp-4`(25행). 여전히 **새 토큰 0개**.
> 없는 이름을 쓰면 **CSS는 아무 말 없이 투명하게 렌더한다**(05 §19-8 마지막 항목). 킷 파일은 한 줄도 고치지 않는다.

### 4-1. 이미 있는 것으로 되는 것 — 새 CSS 0줄

| 화면 조각 | 쓰는 것 |
|---|---|
| 카드·단 폭 | `.diag-wrap`(640 중앙) · `.quiz-card` — 시작·레벨·결과·실패 **네 상태가 같은 카드 한 장** |
| 제목 줄 | `.step-caption` + `.step-title` (시작·결과·실패 카드) |
| 레벨 목록의 **알약·라디오** | `.quiz-choices` / `.quiz-choice` + `<input type="radio">` — 면 `--surface-inset`, `--r-pill`, 세로 스택 |
| 고른 표시 | 기존 `:has(input:checked)` → `--point-soft` |
| 안내 문단 | 클래스 없는 `<p>`(2단 본문) |
| 3단 한 줄(저장 안 됨·**남은 수**·사유) | `.quiz-note` — 남은 수 줄은 3차에서 **문구만** 바뀌고 급·톤은 그대로다. **막힌 문항 안내 줄에는 쓰지 않는다**(§4-2 ⑤ — `--muted`는 "안 띄어도 되는 각주" 급이라 훑어 찾는 조건과 반대다) |
| 진행 줄 | `.quiz-progress` |
| 지문·kana 줄·보기 | `.quiz-prompt` · `.quiz-prompt-sub` · `.jp` |
| [모르겠어요] 구분선 | 기존 `.diag-question .quiz-choices > .quiz-choice:last-child::before` |
| 결과 머리글 | `.diag-headline` |
| 판정표 | `.k-tablewrap` + `.k-table` + 기존 `.diag-wrap .k-table td { tabular-nums }` |
| 준비중 한 줄 | `.muted-text` |
| 버튼 줄 | `.quiz-result-actions` (≤768px 세로 100% 포함) + `.k-btn--primary/--secondary/--ghost` |
| 로딩 | `.k-skeleton` + `.sk-line w40/w70` + `.sk-row` |

### 4-2. 새로 필요한 것 — 새 클래스 4개 + 기존 규칙 1줄 수정 *(+ 3차: 새 클래스 1개 · `.diag-question` 속성 1줄 — 아래 ⑤·⑥)*

새 클래스: `.diag-levels` · `.diag-level-mark` · `.diag-group` · `.diag-group-title`

**① `.diag-levels`** — 시작 화면의 레벨 선택 `<fieldset>`. **값을 새로 쓰지 않는다.**
`.diag-question`이 이미 가진 라벨형 보기 규칙의 **선택자 목록에 `.diag-levels`를 더한다**(6개 규칙):

```css
.diag-question, .diag-levels { border: 0; margin: 0; padding: 0; min-width: 0; }
.diag-question > legend, .diag-levels > legend { /* 기존 3단 라벨 값 그대로 */ }
.diag-question .quiz-choice, .diag-levels .quiz-choice { display:flex; align-items:center; gap:var(--sp-3); }
.diag-question .quiz-choice input[type="radio"],
.diag-levels   .quiz-choice input[type="radio"] { flex:0 0 auto; margin:0; accent-color:var(--point); }
.diag-question .quiz-choice:hover, .diag-levels .quiz-choice:hover { box-shadow:none; }
.diag-question .quiz-choice:focus-within, .diag-levels .quiz-choice:focus-within { box-shadow:var(--sh-point); }
.diag-question .quiz-choice:has(input:checked),
.diag-levels   .quiz-choice:has(input:checked) { background:var(--point-soft); }

/* 안내 문단과 목록 사이 숨 — 이 클래스가 새로 갖는 유일한 값이다.
   ⚠️ 위 리셋(margin:0)과 특이도가 같으므로 **반드시 뒤에 둔다** — 순서를 바꾸면 조용히 사라진다 */
.diag-levels { margin-top: var(--sp-5); }
```

**② `.diag-level-mark`** — 이미 친 레벨 줄의 판정 한 낱말. **알약 안 오른쪽 끝, 3단 라벨.**

```css
.diag-levels .quiz-choice > .diag-level-mark {
  margin-left: auto;            /* 알약이 이미 flex다 — 오른쪽 끝으로 */
  font-size: var(--t-sm);       /* 알약 본문(--t-md)보다 한 급 아래 */
  color: var(--muted);          /* 05 §19-2 보조 텍스트 최소선. --muted2 금지 */
}
```

굵기는 지정하지 않는다 — 알약의 `--w-bold`를 그대로 상속받는 3단 라벨이다. **면·배지·의미색을 쓰지 않는다.**

**③ `.diag-group` / `.diag-group-title`** — 레벨 화면의 갈래 묶음.

```css
/* 갈래 사이에만 선이 있다. 첫 갈래 위에는 없다 — 빈 갈래를 미렌더해도 선이 뜨지 않는다 */
.diag-group + .diag-group {
  margin-top: var(--sp-8);
  padding-top: var(--sp-8);
  border-top: var(--bw) solid var(--border);
}
/* legend(--t-sm·--muted)와 지문(--t-xl) 사이 — 크기·명도 두 축으로 갈린다 */
.diag-group-title {
  margin: 0 0 var(--sp-3);
  font-size: var(--t-md);
  font-weight: var(--w-bold);
  color: var(--text);
}
```

**④ 기존 규칙 1줄 수정** — 문항 사이에서 **선을 뺀다**(선은 갈래 사이로 옮겨 갔다):

```css
/* before */  .diag-question + .diag-question { margin-top:var(--sp-6); padding-top:var(--sp-6); border-top:var(--bw) solid var(--border); }
/* after  */  .diag-question + .diag-question { margin-top: var(--sp-6); }
```

**⑤ `.diag-question-help`** *(2026-09-20)* — 막힌 문항의 안내 한 줄. **legend 바로 다음 자식**, 지문 위. 상태 표시라 **글자색 하나**(05 §15-2 "막힌 문항 안내 줄").

```css
/* 막힌 문항 안내 줄 — 상태 표시다(05 §17-2 계열). 면·테두리·아이콘 없이 글자색 하나.
   .k-help--err를 쓰지 않는다: --t-xs(12px)라 회색 카드 위 라벨 최소선(05 §19-7-3) 아래고, 여백도 한 줄 입력 밑에 붙는 값이다 */
.diag-question > .diag-question-help {
  margin: 0 0 var(--sp-2);     /* legend의 margin-bottom(--sp-2)과 같은 간격 — legend → 줄 → 지문이 같은 숨으로 이어진다 */
  font-size: var(--t-sm);      /* 라벨 최소선. 굵기는 지정하지 않는다 — 문장이지 라벨이 아니다(진행 줄·남은 수 줄과 같은 규칙) */
  color: var(--err-text);      /* --err가 아니다 — 틴트 위에서 채도가 튄다(05 §15-1). 밝은/다크 쌍이 tokens.css에 있다 */
}
```

**⑥ `.diag-question`에 속성 1줄** *(2026-09-20)* — 막힌 제출이 데려가는 **스크롤 목적지**. GNB가 sticky(`--nav-h` 62px)라
`scrollIntoView({ block: "start" })`로 올리면 legend와 안내 줄이 **헤더 밑에 묻힌다.**

```css
/* 이동 목적지가 sticky GNB 아래 --sp-4 여백에 닿게. 이것이 없으면 "이동은 했는데 안내가 안 보이는" 상태가 된다 */
.diag-question {
  scroll-margin-top: calc(var(--nav-h) + var(--sp-4));
}
```

리셋 규칙(`.diag-question, .diag-levels { … }`)과 속성이 겹치지 않으므로 **순서는 무관**하다. `.diag-levels`에는 필요 없다(스크롤 목적지가 아니다).

**새 미디어쿼리는 없다.** 640px 한 단이라 ≤768px에서도 배치가 같고, 버튼 줄의 기존 `.quiz-result-actions` 규칙이 세 버튼을 알아서 세로로 세운다.
**`aria-invalid` 문항에 따로 시각 규칙을 만들지 않는다** — `.diag-question-help`가 곧 그 상태의 표시이고(줄은 `aria-invalid="true"`인 동안만 DOM에 있다 — 1:1),
블록에 테두리·면을 더하면 수단이 둘이 된다(기획 §24-2 ②). 킷의 `[aria-invalid]` 테두리 규칙은 `.k-input` 것이라 fieldset에는 닿지 않는다.

### 4-3. ⚠️ 함정 여섯 개 — 여기서 조용히 깨진다

1. **`.diag-levels`를 [모르겠어요] 구분선 규칙에 더하지 마라.**
   `.diag-question .quiz-choices > .quiz-choice:last-child(::before)`에 `.diag-levels`를 더하면
   **마지막 레벨(고급 N1) 위에 선이 그어진다.** 같은 클래스를 세 화면이 공유할 때의 기본 실수다.
   같은 이유로 `.diag-question + .diag-question` 여백 규칙에도 더하지 않는다(레벨 목록은 fieldset이 한 장뿐이다).
2. **선택자를 `.quiz-choice:has(input[type="radio"])`로 일반화하지 마라.**
   특이도가 (0,2,0) → (0,2,1)로 올라가 **유닛 확인 문제·자료실 퀴즈 쪽 버튼 규칙과의 우선순위가 조용히 뒤집힌다**(05 §19-7-2와 같은 계열).
   화면 전용 규칙은 화면 전용 클래스 안에 둔다.
3. **`.diag-levels { margin-top }`은 리셋(`margin:0`)보다 뒤에 둬라.**
   특이도가 같으므로 순서가 이긴다. 앞에 두면 여백이 조용히 사라진다.
4. *(3차)* **막힌 문항 안내 줄에 킷 `.k-help--err`를 그대로 쓰지 마라.** `--t-xs`(12px)라 회색 카드 위에서 안 읽힌다(05 §19-7-3).
   `.diag-question` 안에서 크기만 덮어쓰는 것도 하지 않는다 — 공용 클래스에 화면 전용 규칙을 얹는 일이다(함정 2와 같은 계열). `.diag-question-help`를 쓴다.
5. *(3차)* **이동 호출 세 가지.** ① `scrollIntoView`에 `behavior: "smooth"`를 넘기지 마라 — 킷 `base.css`의 `prefers-reduced-motion` 처리(`scroll-behavior: auto !important`)를
   우회한다. 기본값이면 CSS가 정하고, 이 프로젝트는 어디에도 `smooth`가 없다(즉시 이동이 규칙 — 05 §15-2). ② 스크롤은 **fieldset**에, 초점은 **첫 라디오**에 —
   초점 호출은 `focus({ preventScroll: true })`가 아니면 라디오 기준으로 **다시** 스크롤해 안내 줄이 위로 밀린다. ③ §4-2 ⑥ `scroll-margin-top`이 없으면 legend가 GNB 밑이다.
6. *(3차)* **없는 id를 가리키는 `aria-describedby`를 남기지 마라.** 남은 수 줄(미응답 0)·안내 줄(답함)은 **요소째** 사라지므로, 버튼·fieldset의 속성도 **함께** 뗀다.
   `aria-invalid`도 같다 — 답하면 `"false"`로 바꾸는 게 아니라 속성을 뗀다(줄과 1:1).

### 4-4. 마크업 요약 (요소 선택)

| 블록 | 요소 |
|---|---|
| 레벨 선택 | `<fieldset class="diag-levels">` + `<legend>` + `<div class="quiz-choices">` + `<label class="quiz-choice"><input type="radio" name="diag-level">…</label>` |
| 이미 친 레벨 표시 | 라벨 안 마지막 자식 `<span class="diag-level-mark">` |
| 갈래 묶음 | `<section class="diag-group">` + `<h2 class="diag-group-title">` — **중첩 `<fieldset>`으로 만들지 않는다**(문항마다 이미 fieldset/legend가 있어 낭독기가 그룹을 두 겹으로 읽는다) |
| 문항 | 기존 `<fieldset class="diag-question">` + `<legend>{번호}. {질문}</legend>` 그대로. *(3차)* **`role="radiogroup"`** 을 준다 — ARIA 1.2에서 `aria-invalid`는 `radiogroup`에는 허용되고 fieldset 기본 역할 `group`에는 **허용되지 않는다**(같은 `name` 라디오 다섯 개라 뜻도 맞다). **막힌 뒤 미응답인 동안만** `aria-invalid="true"` + `aria-describedby="{안내 줄 id}"` — 답하면 둘 다 뗀다. 라디오 다섯 개마다 붙이지 않는다(보기를 옮길 때마다 같은 문장을 다섯 번 듣는다). 최종 요소·속성 확정은 senior-dev(기획 D25) |
| *(3차)* 막힌 문항 안내 줄 | legend **바로 다음 자식** `<p class="diag-question-help" id="diag-q{문항 id}-help">답을 골라 주세요 — 모르면 [모르겠어요]를 고르면 돼요.</p>` — 시도 전·답한 뒤에는 **DOM에 없다**(A50). `role="status"`·`aria-live` 없음 — 초점 이동이 알린다 |
| *(3차)* 남은 수 줄 | `<p class="quiz-note" id="diag-remaining">` — 미응답 0이면 **요소째** 없다. `role="status"` 없음(클릭마다 떠든다) |
| *(3차)* 제출 버튼 | `.k-btn--primary`, **`disabled` 없음**, 남은 수 줄이 있을 때만 `aria-describedby="diag-remaining"`. 막힘 시 초점이 옮겨 가는 요소는 **첫 미응답 문항의 첫 라디오** |
| 판정표 | `.k-tablewrap` > `.k-table`, 레벨 열은 `<th scope="row">`, 가운데는 **`정답 수 / 문항 수`를 한 칸에** |

---

## 5. 05 §19-8 체크리스트 통과 확인 *(이 설계로 한 항목씩)*

| 항목 | 이 설계에서 |
|---|---|
| 읽어야 할 글이 가장 크고 진한가 | 레벨 화면: 지문 `--t-xl` > 보기 `--t-md` > 갈래 제목 `--t-md`(색으로 물러남) > legend `--t-sm --muted`. 결과: `.diag-headline` 26px가 최대 |
| 굵기를 제목에만 썼나 | 이 화면은 **지문을 굵히지 않기로** 이미 정했다(일본어 자형). 비어 있는 굵기 축을 라벨(legend·갈래 제목·알약)이 쓰고, 위계는 **크기·명도**가 만든다 |
| 보조 텍스트가 `--muted`인가 | `.quiz-note` · legend · `.diag-level-mark` 전부 `--muted`. **`--muted2` 0곳.** *(3차)* `.diag-question-help`는 `--err-text` — 보조 텍스트가 아니라 **상태 표시**라 의미색이 맞다(05 §2 보조규칙 · §17-2) |
| 카드가 페이지와 다른 면인가 | `.quiz-card` = `--card-bg`(`--surface-alt` 회색), 페이지 `--bg` 흰 |
| 면을 칠했다면 그 위 색을 전부 봤나 | **새로 칠한 면이 0개다.** 레벨 목록·갈래 어디에도 새 면을 만들지 않았다(§19-7 회피의 가장 확실한 방법) |
| 같은 종류 중 한 장만 튀지 않는가 | 레벨 6줄·문항 6장이 여백·크기·보기 개수까지 전부 같다. 갈래가 달라도 문항 블록은 같다. *(3차)* 막힌 문항 안내 줄은 **강조가 아니라 상태**다 — 미응답은 전부 같은 줄, 답한 문항은 전부 없음(같은 상태 → 같은 표시). 블록의 면·테두리·번호·보기는 그대로 |
| 강조 수단이 하나뿐인가 / hover와 겹치지 않는가 | 선택 = 면 `--point-soft` 하나. hover는 글자색 `--point`뿐이고 **그림자는 꺼져 있다** — 겹치지 않는다. *(3차)* 미응답 = **글자색 한 줄** 하나(면·테두리·아이콘 없음) |
| 길이가 다른 글 아래 요소가 같은 높이인가 | 세로 스택이라 맞출 상대가 없다. 리듬은 `--sp-6`·`--sp-8` 고정 여백이 만든다(kana 줄 유무를 맞추지 않는 판단과 같다) |
| 아이콘·글리프가 옆 글과 높이가 맞는가 | 네이티브 라디오 + `align-items:center`. 커스텀 글리프를 만들지 않았다 |
| 헤더·본문 왼쪽 선이 맞는가 | `.diag-wrap` 640px 한 단 — 모든 줄이 같은 왼쪽 선이다 |
| 같은 정보를 두 번 말하지 않는가 | ~~판 1개일 때 판정표를 그리지 않는다~~(2026-09-15 폐기 — 행이 하나여도 그린다, §1-3·§3-4) · 번호를 갈래마다 되돌리지 않는다 · 시작 화면에 판정표를 얹지 않는다 |
| 감춘 것을 낭독기는 읽는가 | 감춘 것이 없다. **미렌더**만 있다(빈 갈래·없는 버튼·준비중 레벨) — 화면에도 DOM에도 없다. *(3차)* 안내 줄·남은 수 줄도 렌더/미렌더로만 오가고, 렌더될 때는 `aria-describedby`로 fieldset·버튼에 **연결**된다(A55) |
| 인셋에 `--surface-inset`을 쓰는가 | 알약이 그 토큰만 본다. **중간에 면을 끼우지 않았으므로** 상속 계산이 그대로 맞는다 |
| 공용 기본값 특이도가 0인가 | 공용 규칙을 새로 만들지 않았다. `.diag-levels`는 **선택자 목록에 더하는** 방식이라 기존 특이도가 변하지 않는다 |
| 라벨이 `--t-sm` 이상인가 | legend·`.quiz-note`·`.diag-level-mark`·*(3차)* `.diag-question-help` = `--t-sm`(최소선). 그 아래로 내려간 글자 없음 — 킷 `.k-help--err`(`--t-xs`)를 **그래서** 쓰지 않았다 |
| 새로 쓴 토큰이 실재하는가 | §4 머리의 ⚠️ 목록에서 `tokens.css`·`project-tokens.css`를 직접 확인했다. *(3차)* `--err-text` `--nav-h` `--sp-4`도 확인. **새 토큰 0개** |
| 이유가 사라진 규칙을 남겼나 | 계단 모델 문장 4개를 지웠다: *"최대 {n}단계까지 올라갑니다"* · *"제출하고 다음 단계로"* · *"{레벨} 단계는 …여기까지로 판단했어요"* · 첫 단계/2단계 분기 |

---

## 6. 아직 안 정해진 것 — 기획(product-planner) · senior-dev 몫

designer는 **배치·형태·상태**만 정했다. 아래는 자리만 잡아 뒀다.

| # | 항목 | 누구 |
|---|---|---|
| 1 | 결과 머리글 문구 (통과 *"이 레벨은 충분해요"* / 미달 톤 — **낙인이 되면 안 된다**) | product-planner |
| 2 | 판정표 **판정 열의 낱말** — 현행 `—`는 이제 잡음이다(05 §8 "`─`만 늘어선 열" 계열). 통과/미달 둘 다 낱말로 | product-planner |
| 3 | 버튼 라벨 4종 ([다음 레벨 도전하기] / [이 코스로 시작하기] / [한 단계 아래로 해보기] / [다시 진단하기]) | product-planner |
| 4 | 시작 화면 안내 3줄 · legend *"어느 레벨부터 볼까요"* | product-planner |
| 5 | 진행 줄에서 **"단계"라는 낱말** — 계단 모델의 잔재다. 화면 규칙은 "레벨 이름 + 만들어진 문항 수, 분모 없음" | product-planner |
| 6 | **갈래의 정의**(어느 문항 유형이 문법이고 어느 게 어휘인가)와 갈래 이름 3종 | product-planner + 09 |
| 7 | 미달 시 **추천 코스 판정 규칙**(고른 레벨의 코스인가, 한 단계 아래인가) | senior-dev(09 §3-2) |
| 8 | 같은 레벨 재도전 시 **행을 덮는** 규칙의 데이터 표현 | senior-dev(09 §3) |

---

## 7. 이 문서를 지울 때

- [ ] frontend-dev 구현 완료 + senior-dev 코드리뷰 통과
- [ ] *(3차)* 미응답 필수(A48~A56) 구현 완료 — `.diag-question-help` · `scroll-margin-top` · aria 연결이 §4-2 ⑤·⑥·§4-4대로
- [ ] qa 판정 통과
- [ ] §6의 미확정 문구가 전부 확정되어 코드에 반영됨
- [ ] **새로 생긴 "다음 화면에도 쓰이는 규칙"이 `설계/05`로 올라감** — 지금 기준으로는 05 §15-2에 이미 전부 있다
- [ ] 그 뒤 이 파일을 삭제하고 `진행사항/README.md`에서 줄을 지운다
