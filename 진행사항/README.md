# 진행사항 — 지금 무슨 상태이고 다음에 뭘 하나

이 폴더에는 **지금 진행 중이거나 계속 변하는 것**만 둔다.
프로젝트 내내 유효한 규칙·계약은 **`../설계/`** 에 있다.

기준일: **2026-09-16**(진단 3차 기획 — 미응답 필수) / 기준 커밋: `17e19c3` 이후 (전체 감사 + 문서 정리 → 진단 1차·2차 개편)

---

## 1. 지금 어디까지 왔나

| 영역 | 상태 |
|---|---|
| 학습 코스(일본어) | ✅ **6개 전부 개통** (입문·N5·N4·N3·N2·N1) — 유닛 125, 문법 295, 한자 1,350, 어휘(표제어) 1,734, 회화 125 |
| 학습 코스(영어) | ⚠️ **뼈대만** — 코스 5개(101~105), 맛보기는 코스 101의 유닛 2개. 표현 12건. **확인 문제·음성은 의도적으로 없다** |
| 자료실 | ✅ 3종(한자·문법·어휘) + 자료실 퀴즈 · 영어 3탭(표현·문법·어휘) |
| 학습 도구 | ✅ 유닛 확인 문제 · 실력 진단 · 음성(TTS) — 동작 규칙은 `../설계/09` |
| 활용 규칙표 | ⚠️ 문법 295개 중 **87개 보유(359행)** — 나머지 208개는 `rules: []`(서술형은 없는 게 정상) |
| 개인 데이터 | ✅ 진도 저장 · 보관함 (**비로그인 포함**) + 로그인 시 합치기 |
| 계정·인증 | ✅ 로컬 + 카카오·구글, JWT 쿠키 + 리프레시 회전(`jti`) · 로그인 시도 제한 5회/15분 |
| 커뮤니티·파일 | ✅ 동작 (보일러플레이트 유래, 학습과 연결 안 됨) |
| 콘텐츠 편집 | ⚠️ **로컬 전용** — 운영에서 켜면 기동 거부 |
| 테스트 | ✅ **프론트 687 전부 통과**(2026-09-15, 진단 2차 개편 후) · **백엔드 285**(2026-09-11 기준 — 2차 개편은 화면·lib만 바뀌고 새 API가 없다, 기획서 §20 ④) · lint error 0 |
| CI | ✅ `.github/workflows/ci.yml` — **원격 미연결이라 아직 실행된 적은 없다** |
| 배포 | ❌ 아직 안 했다 (사용자 지시로 보류) |

**방금 끝난 일**: **프로젝트 전체 감사(58건) + 문서 정리.**
일본어 과정만 봤던 1차에 이어 비학습 기능·영어·백엔드/보안을 감사해 고침 19·안 고침 18(근거 기록)로 처리했고,
`진행사항/`의 이터레이션 임시 문서 10개를 **`설계/`로 흡수한 뒤** 지웠다(코드 주석 인용 643건을 함께 이전).

**진행 중인 일**: **디자인 킷 교체 — Halo → Lets** (2026-09-03 착수)
`설계/05_디자인시스템.md`는 **Lets 기준으로 개정 완료**(§1~§14 재작성, §15~§18은 제품 규칙이라 표현만 갱신).
코드 전환은 아직이다 — 작업 지시서는 **[`디자인전환_Lets_매핑.md`](디자인전환_Lets_매핑.md)** 이고 담당은 frontend-dev.
전환이 끝나면 그 문서를 지운다.

**끝난 일**: **실력 진단 개편 2개 차수 — 구현 완료.**
- **1차**(2026-09-10 기획 → 09-11 구현, 커밋됨): 레벨당 6문항 한 화면 제출 · 레벨별 표기 규칙 · [모르겠어요].
- **2차 "한 레벨 = 한 판"**(2026-09-14 기획 → **2026-09-15 구현 완료** — vitest 687/687, senior-dev 리뷰 통과, 수정 필수 결함 0):
  계단(입문→N1 자동 진행)을 없애고 **사용자가 시작 레벨을 고른다.** 제출하면 통과든 미달이든 **결과 화면에서 멈추고**,
  이어서 할지는 매번 사용자가 정한다. 한 판 안에서는 문항이 **갈래(어휘·문법·어법·문장)별로 묶여** 보이고,
  추천은 **통과한 레벨 기준**으로 다시 계산한다(*N2 통과 → N5 추천* 회귀는 `diagnosis.recommend.test.js`가 막는다).
  기획서는 **[`기획_2026-09_진단개편.md`](기획_2026-09_진단개편.md) §11~§20**. 규칙 원본은 **`설계/09 §3`**(동작) · **`설계/05 §15-2`**(화면) · **`설계/08 F-24~F-29`**(뒤집힌 전제).
  §16 AC 체크는 테스트 인용을 열어 확인한 것만 — **A36(새로고침 → 기록 삭제)만 미확인**(테스트 없음).

**진행 중인 일 (2026-09-16 착수)**: **실력 진단 3차 — 미응답 필수.** 사용자 요구(*"빈칸으로 내면 틀리게 처리됨 — 실수로 건너뛴 걸 수도, 필수체크 해주삼"*).
[모르겠어요]가 보기로 있으므로 빈칸은 실수다 → **미응답이 있으면 제출되지 않는다.** 막는 방식은 회색 버튼이 아니라 **누르면 첫 미응답 문항으로 이동·안내**.
기획서 **[`기획_2026-09_진단개편.md`](기획_2026-09_진단개편.md) §21~§30**(AC A48~A56 · 무효 A6 · 부분 개정 A11). 채점·결과·추천은 안 바뀐다.
**다음**: senior-dev가 `설계/09 §3-6`을 기획서 §28-1대로 개정하며 테스트 선작성(Red) → frontend-dev → designer는 안내 줄 형태(§28-2 ④) → qa. **08 §F 기록(F-30)은 구현 뒤.**

**진행 중인 일 (2026-09-16 착수, 설계 완료 2026-09-20)**: **SPA 뒤로가기·새로고침 체감** — 사용자 말(*"ssr 사이트에서 되던게 안되는느낌"*).
CSR 안에서 푼다(Next.js 전환 없음): ① `useApiQuery`에 캐시(stale-while-revalidate) ② `<BrowserRouter>` 유지 + `useScrollRestoration`(PUSH 맨 위·POP 보던 위치·replace 그대로) ③ 27화면 전수 — 새로 URL로 가는 것은 댓글 페이지 `?cpage=` 1건.
설계·전수 표·인계는 **[`기술설계_2026-09_SPA_상태복원.md`](기술설계_2026-09_SPA_상태복원.md)**, 규칙은 `설계/02 §4`·`05 §8·§9`·`08 C-23`. Red 테스트 5파일 작성됨 → **frontend-dev 구현 차례**(진단 3차와 파일이 겹치지 않는다). 진단 세션 보존(sessionStorage)은 **제안만**(그 문서 §4) — 사용자 확인 뒤 별도.

**진단 개편의 남은 순서**:
0. **3차 구현**(위) — 이게 끝나야 아래 qa가 한 번에 된다(2차 AC + 3차 AC).
1. **qa** — 기획서 §16 AC(유효분 A3~A47, 단 A6 제외)와 **§26 A48~A56**을 체크리스트로 실물 시험. `QA_확인가이드.md` §2-6 · B-6 · C-4 · C-10 · C-11도 새 흐름으로 맞춰 두었다. **A36은 자동 테스트가 없으니 손으로 본다.**
2. qa 통과 후 **`화면정의_2026-09_진단.md` 삭제**(문서 머리의 폐기 조건 "구현·QA가 끝나면" — 지금은 **QA 후 폐기 예정**으로 둔다). 기획서도 같은 시점에 지운다(규칙은 이미 `설계/`에 있다).
3. 사용자가 요청하면 **devops** 커밋.
4. 딸린 정리: `설계/08` F-24 머리의 *"코드는 2026-09-14 현재 아직 구판이다"* 한 줄이 낡았다 — senior-dev가 지운다.

---

## 2. 다음 순서

### 배포하려면 반드시 (아직 안 함)

1. **운영 시드 적재** — 운영은 `sql.init.mode: never`라 콘텐츠가 자동으로 안 들어간다.
   적재하지 않으면 사이트는 뜨지만 **코스 목록이 텅 빈다.** → `../설계/07 §1`
2. **`railway-deploy` 스킬 채우기** — 실질 절차는 `07`에 있다.
3. **기동 거부 3조건 확인** — 편집 모드 `false` · **H2 콘솔 `false`** · 버킷 환경변수. → `../설계/07 §1-1`
4. **배포 시점 QA에서 비밀번호 최소 4자 재검토** — 기한 있는 유예다(`../설계/08 F-23`).
   **운영 계정이 0개인 지금이 바꾸기 가장 싸다.**

### 기능 우선순위 (제안)

| 순위 | 항목 | 왜 |
|---:|---|---|
| 1 | **배포** | 만든 것이 아직 아무에게도 안 보인다 |
| 2 | **영어 콘텐츠 채우기** | 뼈대만 있다. ⚠️ **첫 작업은 콘텐츠가 아니라 Red 테스트 5개** — 영어 콘텐츠 규칙을 강제하는 테스트가 **0건**이다(`../설계/06 §11-11`) |
| 3 | **문법 활용표 보강** | 295개 중 87개. 늘릴지 말지는 판단 — 서술형에 붙이는 건 오히려 해롭다 |
| 4 | **오답노트·학습 통계** | 퀴즈·진단 결과를 저장하지 않기로 한 F-21을 뒤집는 결정이 먼저다 |

> **새 기능을 만들 때**: 인증이 필요한 기능은 `/api/courses/**`·`/api/library/**`·`/api/en/**`(GET 전면 공개) 아래에 두지 않는다.
> `/api/progress/**`·`/api/bookmarks/**`·`/api/me/**` 옆에 만들고 동시 쓰기 규칙도 따른다. → `../설계/08 B-8~B-11`

> **AC-P-24(자료실 링크 새 탭 → 같은 탭)는 아직 미전환이다.** 스텝이 URL이 아니라 컴포넌트 state라
> 복원이 `lastPosition`에만 의존한다. 전환할 때 스텝을 주소 쿼리에 담을지 함께 판단한다.

---

## 3. 이 폴더의 문서

| 문서 | 내용 |
|---|---|
| **README.md** (이 문서) | 현재 상태 + 다음 순서 |
| **현재_기능현황.md** | 구현 전수 목록 + 아직 없는 것 (코드·API 응답 대조) |
| **QA_확인가이드.md** | **사용자가 직접 브라우저로 확인하는 법** — 화면 지도 · 화면별 동작 · 체크리스트(되돌아온 결함 10건 포함) · 제보 양식 |
| **작업정리_2026-08-25.md** | 무엇을 왜 만들었나 — 중요한 판단의 서술형 정리 |
| **N1_한자350_확정목록.md** | N1 한자 350자 (기존 1,000자와 교집합 0 검증본) — **시드가 참조한다** |
| **미결_사람결정.md** | 열린 항목 **14건**(전부 기본값 있음 — 진단 1차 Q1~Q6 · 2차 Q7~Q11 · 3차 Q12~Q14). 그 외는 전량 확정 |
| **기획_2026-09_진단개편.md** | 실력 진단 개편 기획서 **3개 차수**. **§0~§10 = 1차(2026-09-10, ✅ 구현 완료)** — 레벨당 6문항 한 화면 제출 · 레벨별 표기 규칙 · [모르겠어요](AC 20개, 무효분은 그 자리에 표시). **§11~§20 = 2차(2026-09-14 기획, ✅ 2026-09-15 구현 완료)** — 시작 레벨 직접 선택 · 통과/미달 모두 결과 화면에서 정지 · 갈래별 문항 묶음(AC 27개 + 무효 대응표 §17 + senior-dev 인계 §20). §16 체크는 테스트 인용을 열어 확인한 것만 — **A36 미확인**(qa가 손으로 본다). **§21~§30 = 3차(2026-09-16 기획, 📝 구현 대기)** — **미응답 상태로는 제출되지 않는다**(누르면 첫 미응답 문항으로 이동·안내. AC A48~A56 · 대응표 §27 · 09/05에서 바뀔 문장 §28 · 인계 §30). 규칙은 구현과 함께 `설계/`로 가니 **qa 통과 후 지운다** |
| **화면정의_2026-09_진단.md** | 진단 2차 개편의 **구현 인계용 화면정의서**(designer, 2026-09-14, ✅ 구현 완료 · **QA 후 폐기 예정**) — 와이어프레임 4종 · 상태별 UI 전수 표 · 탈락 시안 비교 · **CSS 선택자·속성 목록(새 토큰 0개)** · `05 §19-8` 통과 확인. **규칙의 원본은 `../설계/05 §15-2`·`§19`** 이고 이 문서는 그 적용 결과다 — 문서 머리의 폐기 조건은 "구현·QA가 끝나면"이고 **qa가 아직이라 그대로 둔다** |
| `감사_2026-08-25_*.md` (4건) | 감사 원본 — 일본어 / 비학습 기능 / 영어·자료실 / 백엔드·보안 |
| `감사판정_*.md` · `판정_*.md` (2건) | 판정 — **안 고친 18건의 이유가 여기에만 있다.** 다음 감사의 기준선 |

> 이터레이션 임시 문서(기획서·화면정의서·계약·실라버스)는 **2026-08-31에 전부 삭제**했다.
> 내용은 `설계/03·04·05·06·09`가 흡수했고, 코드 주석 인용 643건도 함께 옮겼다.

---

## 4. 영구 지식은 어디에

| 알고 싶은 것 | 문서 |
|---|---|
| 무엇을 왜 만드나 | `../설계/01_제품개요.md` |
| 스택·빌드·실행 | `../설계/02_아키텍처.md` |
| 테이블·시드·ID 대역 | `../설계/03_데이터모델.md` |
| API 계약 | `../설계/04_API계약.md` |
| 화면 규칙 | `../설계/05_디자인시스템.md` |
| 콘텐츠 만드는 법 | `../설계/06_콘텐츠_제작규칙.md` (영어는 §11) |
| 배포 | `../설계/07_운영_배포.md` |
| "왜 그렇게 했나" | `../설계/08_결정기록.md` |
| **퀴즈·진단·음성 동작 규칙** | `../설계/09_학습도구_동작규칙.md` |

---

## 5. 이 폴더의 규칙

1. **끝난 일은 지운다.** 단, **`설계/`로 옮기고 코드 주석 인용까지 바꾼 뒤에** 지운다.
   이 순서를 어기면 규칙이 조용히 사라진다 — 2026-08-31 정리에서 흡수 누락 5건이 이 순서 덕에 발견됐다.
2. **계속 유효한 규칙을 발견하면 `설계/`로 올린다.** 진행 문서에 규칙을 묻어 두지 않는다.
3. **상태가 바뀌면 이 README부터 고친다.** 여기가 "지금 뭐 하는 중인가"의 단일 출처다.
4. **수치는 코드·API 응답에서 센다.** 문서에서 문서로 베끼면 3단계 뒤처진 문서가 남는다.
