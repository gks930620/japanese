# 확정 — 사람 결정 항목 (2026-08-20 전량 종결 · 2026-08-26 사용자 결정 1건 추가 종결)

> **이 문서는 더 이상 "미결" 문서가 아니다.**
> 사용자 지시 **"묻지 말고 다 yes로 해"**(2026-08-20)에 따라, 대기 중이던 항목을 senior-dev가 **전부 판정해 닫았다**.
> **되묻는 항목은 하나도 없다.** 작업은 어떤 항목 때문에도 멈추지 않는다.
>
> **판정 전문(확정 / 근거 / 뒤집는 법)은 `../설계/08_결정기록.md` §F** 에 있다 — 같은 내용을 두 곳에 두지 않는다.

## 요약

| 구분 | 항목 |
|---|---|
| **확정** (F-1~F-18, F-21·F-22) | 사이트명 유지 · 한자 기준=상용한자표 · 786자 미약속 · N1 독해 제외 · 실라버스 현행 진행 · 품사 7종 유지 · 어휘 하한 비차등 · 획수/부수 미도입 · 회화 자료실 미도입 · "내 보관함" · 닉네임 중복 허용 · 탈퇴 보관 최소화 · 진단 3중 2 · 유닛 10문항 · 브라우저 음성 수용 · 편집 절차 문서화 · 영어 코스명·발음 표기 · 퀴즈 결과 미저장 · 앱 문구 |
| **소멸** (F-19·F-20) | 방장 탈퇴 방 처리(채팅 제거) · 아이디/비밀번호 찾기(D-3) |
| **designer 소관** (사람 결정 아님) | 입문 준비중 문구 · N2 톤 화면 반영 · 문법 필터 "기타" 라벨 · 코스 완료 연출 · 유닛 스텝 8개 밀도 · 진단 배너 위치 |

## 2026-08-26 추가 확정 — 사용자가 직접 답한 항목

| 항목 | 확정 | 전문 |
|---|---|---|
| 운영에서 기본 회원가입을 닫을 것인가(보안 감사에서 올린 건) | **닫지 않는다 — 기본 회원가입 + 소셜 로그인 둘 다.** 지금은 로컬 개발 단계로 보고 **같은 코드**가 로컬·운영에서 다 되게 만든다. 운영 동작 확인은 **배포 시점 QA** | `설계/08` **F-23** · 원칙은 **C-17** |
| (딸림) 비밀번호 최소 4자 | **지금 고치지 않는다.** 배포 QA에서 재검토 — 지금 방어선은 로그인 시도 제한 5회/15분 | `설계/08` §F(F-23 딸린 항목) |

> 이 건은 되묻지 않고 확정한 것이 아니라 **사용자가 직접 답한 것**이다. 그래서 C-10(기본값 확정)이 아니라 **사용자 결정**으로 기록한다.
> 이로써 이 문서에 열린 항목은 **다시 0건**이다.

## 2026-09-03 — 사람 결정이 필요한 항목 2건 (세 점검 통합 판정에서 올림)

> 둘 다 **C-10 기본값 확정으로 닫을 수 없는 종류**다 — 하나는 로컬 개발 편의를 깎고, 하나는 이미 사용자가 "지금은 유예"라고 답한 값이다.
> 결정 전까지 작업은 멈추지 않는다: 아래 "기본값"으로 진행한다.

| 항목 | 무엇을 정하나 | senior-dev 기본값(결정 전) | 근거·전문 |
|---|---|---|---|
| **기본 프로파일의 안전 기본값** (점검 M5) | `application.yml`(기본 프로파일)이 `spring.h2.console.enabled: true`·`app.editor.enabled: true`를 켠 채라 방어가 전부 `prod` 프로파일에 매달려 있다(`RailwayDeploymentValidator`도 `@Profile("prod")`). **기본을 "꺼짐"으로 뒤집고 로컬만 켤 것인가**(예: `application-local.yml` + `bootRun`에 local 고정) | **유지.** 지금 배포 경로는 Dockerfile 하나뿐이고 prod가 고정돼 있어 당장 위험이 없다. 뒤집으면 `bootRun` 사용법이 바뀐다(02 §5-1 갱신 필요) | `점검_2026-09_설계로직.md` M5 · 07 §1-1 경고 |
| **비밀번호 최소 길이 4 → 6?** (점검 D-9, 08 F-23 딸린 항목) | 08 F-23은 "배포 QA에서 재검토"로 유예했고 **지금이 그 시점**이다(운영 계정 0개인 지금이 가장 싸다). 값을 정하면 **세 곳**(가입 안내·변경 안내·`JoinDTO @Size`)을 같이 바꾼다 | **4 유지.** 화면 문구만 서버와 같게(6→4) 고친다 — `pages/SignupPage.test.jsx`가 "화면 = 서버 값"을 고정한다. 6으로 정하면 테스트의 `SERVER_PASSWORD_MIN`과 `JoinDTO`를 같이 올린다 | `점검_2026-09_사용자관점.md` D-9 |

## 2026-09-10 — 실력 진단 개편에서 올라온 항목 6건 (기획서 §10)

> 전문·기본값은 **[`기획_2026-09_진단개편.md` §10](기획_2026-09_진단개편.md)** 에 있다 — 같은 내용을 두 곳에 두지 않는다.
> 여섯 건 모두 **기본값이 정해져 있어 작업은 멈추지 않는다.**
> **2026-09-11 구현은 여섯 건 다 기본값대로 나갔다.** 사용자가 실물을 보고 뒤집으면 그때 바꾼다(Q2는 숫자 하나, 나머지는 규칙 한 줄이다).

| # | 무엇을 정하나 | 기본값 |
|---|---|---|
| Q1 | [모르겠어요] 보기를 유닛 확인 문제·자료실 퀴즈에도 넣을까 | **진단에만** |
| Q2 | 레벨당 문항 수 6이 맞나 | **6** |
| Q3 | 입문을 진단 계단에 넣을까 | **넣는다** |
| Q4 | N2·N1 빈칸 문항에 한국어 해석 줄을 남길까 | **남긴다** |
| Q5 | N5에서 조사 문법(`助詞「は」` 등)이 지문에서 빠지는 것을 받아들일까 | **받아들인다** |
| Q6 | 진단이 가타카나를 묻지 않는 구간(입문·N5·N4)을 받아들일까 | **받아들인다** |

> ⚠️ 함께 딸린 것: 이 개편은 `설계/08 §F` **F-13(진단 통과 기준 "3중 2" 유지)을 뒤집는다.**
> 사용자가 실물을 보고 요구해 전제가 바뀐 건이므로, senior-dev가 08 §F에 *뒤집힘·새 근거*를 기록한다.
>
> **Q3은 2026-09-14 2차 개편으로 뜻이 바뀌었다** — "입문을 계단에 넣을까"가 아니라 **"입문을 고를 수 있는 레벨에 넣을까"** 다(계단이 없어졌다). 답은 그대로 **넣는다**.

## 2026-09-14 — 실력 진단 **2차** 개편에서 올라온 항목 5건 (기획서 §19-1)

> 전문·근거는 **[`기획_2026-09_진단개편.md` §19-1](기획_2026-09_진단개편.md)** 에 있다.
> 다섯 건 모두 **기본값이 정해져 있어 작업은 멈추지 않는다.** Q7·Q9는 사용자가 말한 것을 기획이 **일부러 다르게 정한 건**이라 뒤집힐 여지가 가장 크다.

| # | 무엇을 정하나 | 기본값 |
|---|---|---|
| Q7 | 결과 화면에 **[끝내기]** 버튼을 명시적으로 둘까 (사용자가 말한 세 행동 중 하나였다) | **두지 않는다** — 자동 진행이 없어 "안 누르는 것"이 곧 끝내기다 |
| Q8 | [다른 레벨 고르기]가 지금까지의 판정 기록을 **유지**하는 게 맞나 | **유지한다** |
| Q9 | 결과 표에 **갈래별 정답 수**(어휘 2/3 · 어법 3/3)를 넣을까 | **넣지 않는다** — 문항별 정오 미표시 규칙(**09 §3-1** — 2026-09-15 인용 정정, 예전 표기 §3-6은 제출 단위 절이었다)과 충돌한다 |
| Q10 | N2·N1 빈칸 문항에 **질문 줄**을 붙일까(지금 문항 머리는 질문이 아니라 예문 뜻이다) | **지금 그대로** — `어법` 머리글이 무엇을 묻는지 말해 준다 |
| Q11 | 한 판을 치고 나면 코스 목록으로 **자동 이동**할까 | **하지 않는다** |

> ⚠️ 함께 딸린 것: 이 개편은 **"진단은 낮은 레벨부터 올라가는 계단이다"** 라는 전제를 뒤집는다(1차의 D6).
> senior-dev가 `설계/08 §F`에 *뒤집힘 · 새 근거(사용자 요구 8) · 요구한 사람*을 기록한다. → ✅ **08 F-24~F-29에 기록됨**(2026-09-14).
>
> **2026-09-15 구현 완료 — 다섯 건 다 기본값대로 나갔다.** product-planner가 확인한 범위: **Q7·Q11**은 `frontend/src/pages/DiagnosisPage.jsx`에 [끝내기] 버튼도 자동 이동(`navigate`)도 없음(코스로 가는 것은 사용자가 누르는 링크뿐) · **Q8**은 테스트 A35 · **Q9**는 테스트 A47. **Q10(빈칸 문항의 질문 줄)은 코드를 열어 보지 않았다.** 사용자가 실물을 보고 뒤집으면 그때 바꾼다.

## 2026-09-16 — 실력 진단 **3차**(미응답 필수)에서 올라온 항목 3건 (기획서 §29)

> 전문·근거는 **[`기획_2026-09_진단개편.md` §29](기획_2026-09_진단개편.md)** 에 있다.
> 세 건 모두 **기본값이 정해져 있어 작업은 멈추지 않는다.** 결정 자체(미응답이면 제출 불가)는 사용자가 직접 요구한 것이라 미결이 아니다 — 아래는 **그 방식의 세부**다.

| # | 무엇을 정하나 | 기본값 |
|---|---|---|
| Q12 | 레벨 고르기 화면에서 "빈칸으로는 제출할 수 없어요"를 **미리** 말할까 | **말하지 않는다** — 규칙은 필요한 자리(문항 화면)에서 말한다 |
| Q13 | 막힌 뒤 안내 줄을 **첫 미응답 문항에만** 붙일까, **미응답 전부**에 붙일까 | **전부** — 3개 건너뛴 사람이 세 번 누르지 않게 |
| Q14 | 제출 버튼 위 남은 수 줄을 **시도 전에도** 보여줄까(지금처럼), **시도 뒤에만** 보여줄까 | **지금처럼 계속** — 버튼에 닿기 전에 아는 편이 헛클릭을 줄인다 |

> ⚠️ 함께 딸린 것: 이 개편은 1차 결정 **D7(미응답 = 모름, 제출을 막지 않는다)** 을 뒤집는다. `설계/08 §F`에는 이 규칙의 독립 항목이 **없으므로** 뒤집히는 08 번호는 없다 —
> senior-dev가 **구현 뒤** 새 항목(F-30)으로 *뒤집힌 전제 · 새 근거(기획서 §21-4) · 요구한 사람(사용자)* 을 기록한다.

## 새 항목이 생기면

1. 먼저 **senior-dev가 기본값으로 확정**하고 `설계/08 §F`에 *확정 / 근거 / 뒤집는 법*을 적는다(C-10).
2. 사용자에게 되묻지 않는다. 뒤집는 비용이 큰 결정만(예: 비용이 드는 외부 서비스 도입) 진행하면서 알린다.
