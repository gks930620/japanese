# 기획 — 영어 코스 1 「다시 세우기」(E1) 커리큘럼

> 작성: product-planner · 2026-09-21
> **폐기 조건**: E1 유닛 15개가 전부 들어가고 qa가 통과하면 이 문서를 지운다.
> 계속 유효한 규칙(유닛 수·회화 줄 수·`notice` 규칙)은 **이미 `../설계/06 §11-12`에 올려 두었다** — 규칙을 찾을 때는 이 문서가 아니라 그쪽을 본다.
> **2026-09-22 추가**: 이번 라운드에서 못 박은 세 규칙도 **이 문서가 폐기돼도 남도록 `../설계/06`에 올렸다** —
> **`§11-7`** 한국어를 먼저 쓰고 영어를 맞추지 않는다 · 회화 장면의 성격은 그 유닛의 문법이 정한다 / **`§11-8`** `name_ko`는 영어를 넣지 않는 한국어 부제 · 설명이 부제를 반박하지 않는다 / **`§11-11`** 사람 검수 항목 7·8.
> **정본은 `설계/06`이다.** 아래 §8-3 16·17·18은 E1 작업용 사본이고, 어긋나면 `설계/06`이 이긴다.
> 여기 남는 것은 **"유닛마다 무엇을 왜 그 순서로 가르치는가"** 와 인수 조건이다.

## 이 문서를 읽는 법

- **backend-dev(영어 콘텐츠)가 첫 독자**다. §3의 유닛 표와 §6의 인계만 읽어도 유닛 3개를 쓸 수 있게 적었다.
- **1·2유닛의 커리큘럼은 다시 설계하지 않는다.** 무엇을 가르치느냐(문법·회화·표현·어휘 목록)는 그대로 두고, 이 문서는 **유닛 3부터** 이어 쓴다.
  단 **문법 설명의 톤은 2026-09-21에 소급 보강하기로 했다** — 커리큘럼 변경이 아니라 *같은 내용을 이 코스의 어법으로 다시 쓰는 것*이다. 판정과 초안은 **§3-3 머리**에 있다.
- 수량·표기·품사 규칙은 전부 **`../설계/06 §11`** 이 정본이다. 이 문서는 규칙을 다시 적지 않고 **무엇을 채울지**만 정한다.

---

## 0. 한 줄 요약

> **E1은 15유닛으로 끝낸다. 지금은 10유닛까지 열렸고, 5의 배수 단위로만 넓힌다**(2 → 5 → **10(현재)** → 15).
> 유닛마다 **"학교에서는 이렇게 배웠는데, 실제로는 이렇게 쓴다"** 는 대비가 문법 설명 안에 반드시 들어간다 — 그것이 이 코스의 정체성이다.

---

## 1. 확정한 것 (원본: `../설계/06 §11-12`)

| # | 미결이었던 것 | 확정 |
|---|---|---|
| 1 | 코스별 유닛 수 | **E1 15 · E2 20 · E3 20 · E4 20 · E5 20 (계 95)**. E1의 **부분 공개는 5 → 10 → 15** |
| 2 | 회화 대사 줄 수 하한 | **E1·E2 4줄 이상 · E3·E4 6줄 이상 · E5 8줄 이상, 전 코스 12줄 이하**(화자 2명 이상은 기존 그대로) |
| 3 | 코스 101 `notice` 문구 | **숫자를 적지 않는다.** `앞 유닛부터 순서대로 채우는 중이에요 — 열려 있는 유닛까지는 지금 그대로 학습하면 됩니다` · **15유닛이 차면 `NULL`로 지운다** |

근거는 `../설계/06 §11-12`에 적었다. 여기서는 **E1 15유닛**의 근거만 한 번 더 적는다 — 커리큘럼 설계와 직결되기 때문이다.

> E1은 **백지에서 시작하는 코스가 아니다.** 대상이 *"단어는 아는데 문장이 안 만들어지는 학습자"* 이므로
> 새로 넣을 것보다 **바로잡을 것**이 많고, 바로잡을 목록은 유한하다.
> "현재 시제로 하고 싶은 말 한 문장을 끝맺는다"에 필요한 규칙은 **문법 38개(15유닛 × 2~3개)** 면 전부 덮인다.
> 20유닛으로 늘리면 **E2(시제)·E3(잇기)의 몫을 당겨써야 하고**, 그러면 코스 경계가 무너진다.

---

## 2. E1의 도착점과 경계 — 무엇을 가르치고 무엇을 안 가르치나

**도착점**(코스 `goal` 그대로): *"학교에서 배운 조각들을 문장 만드는 규칙으로 다시 세워, 하고 싶은 말을 한 문장으로 끝맺을 수 있어요"*

| 이 코스에서 가르친다 | 이 코스에서 안 가르친다 (어디로 가나) |
|---|---|
| 현재 시제 한 문장 — be동사 / 일반동사 | **과거·미래·진행·완료 → E2** |
| 의문·부정·짧은 대답·의문사 | 시제가 섞인 의문·부정 → E2 |
| 문장의 부품 — 명사·관사·대명사·형용사·부사·전치사 | 관계절·비교 → E3 |
| 어순(자리가 곧 조사다) | **두 문장을 하나로 잇기 → E3** (접속사는 E1에서 단어·구를 잇는 데까지만) |
| `can` · `want to` · 명령·제안 | 조동사 뉘앙스·가정·수동·완곡 → E4 |
| `I think ~` 로 말을 끝맺기 | 회의·이메일·격식 → E5 |

> **경계를 넘지 않는 것이 이 기획의 핵심 제약이다.** 유닛 12에서 `Could you ~?`를 가르치되
> "과거형이 아니라 더 정중한 현재"라고만 말하고 **과거 시제 자체를 설명하지 않는다** — 그건 E2의 첫 유닛이다.

---

## 3. 유닛 커리큘럼 (3~15)

### 3-1. 한눈에

| 유닛 | 제목 | 문법 | 회화 장면 | 어휘 테마 |
|---:|---|---|---|---|
| 1 | 한 문장에 동사는 하나 | be동사 현재 · 일반동사 -s | 첫 출근 날, 옆자리에서 | 아침·회사 첫날 |
| 2 | 묻고, 아니라고 답하기 | do/does 의문 · don't/doesn't | 주말에 뭐 하세요? | 주말·취미 |
| **3** | **예·아니오는 되받아서 답한다** | be 의문 · 짧은 대답 · be 부정 | 혹시 새로 오신 분인가요? | 사람·소개·신분 |
| **4** | **무엇을·어디서·언제를 앞에 세운다** | 의문사 의문문 · how+형용사 · who 주어 | 점심은 어디서 드세요? | 식사·거리·시간 |
| **5** | **순서가 곧 조사다** ★복습 | S+V+O · 장소 먼저 시간 나중 · 주어를 빼지 않는다 | 퇴근길 통화 — **이미 잡힌 약속**의 장소·시간 확인 | 이동·장소 |
| **6** | **셀 수 있는지부터 본다** | 셀 수 있는/없는 명사 · 복수 -s · a/an은 소리로 | 장 보러 가는 길 — **적어 온 목록**을 하나씩 확인 | 음식·장보기 |
| **7** | **처음엔 a, 다시 말하면 the** | a와 the의 갈림 · 관사가 없는 자리 · 굳은 the | 그 택배 어디 뒀어요? | 사무·물건 |
| **8** | **자리가 정해 주는 말 — I · my · me** | 인칭대명사 네 모양 · it/they로 받기 · this/that | 이거 제 거예요? — **누구 것인지 확인만** 한다 | 소지품·관계 |
| **9** | **있다고 말할 땐 There로 연다** | There is/are · 그 의문·부정 · 위치는 뒤에 | 이 층에 뭐가 있어요? | 건물·시설 |
| **10** | **시간과 장소에 붙는 at · on · in** ★복습 | 시간의 at/on/in · 장소의 at/on/in · 빈도부사 자리 | **이미 금요일 10시로 잡힌 회의**의 참석·장소 확인 | 날짜·일정 |
| **11** | **꾸미는 말은 앉을 자리가 있다** | 형용사의 두 자리 · 부사 -ly와 예외 · very/really/too | 오늘 좀 춥지 않아요? | 감정·상태·정도 |
| **12** | **할 수 있다와 해 주세요는 같은 단어** | can/can't · Can you?/Can I? · Could you?는 과거가 아니다 | 잠깐 도와주실 수 있어요? | 도움·업무 동작 |
| **13** | **동사를 두 개 쓰고 싶을 때** | want/need to + 원형 · like/enjoy + -ing · have to | 오늘 꼭 끝내야 해요 | 업무·마감 |
| **14** | **시키는 말이 무례한 건 아니다** | 명령문 · Let's/Let me · Why don't we?/How about -ing? | 그럼 이렇게 해 볼까요? | 방향·동작 지시 |
| **15** | **하고 싶은 말을 한 문장으로 끝낸다** ★복습 | I think / I don't think · It's ~ to 동사 | 저는 이게 낫다고 봐요 | 의견·판단 |

★ = `unitNo % 5 == 0` 이라 **정리 스텝에 직전 5유닛 복습 블록이 자동으로 붙는다.** 복습 전용 유닛이 아니다 — 5·10·15도 문법·회화·표현·어휘를 정상으로 채운다.

> ⚠️ **회화 장면의 성격에는 경계가 있다 — 유닛 11과 12 사이다**(규칙 전문은 §3-4 ③).
>
> | 유닛 | 회화 장면의 성격 | 왜 |
> |---|---|---|
> | **1~11** | **이미 정해진 일의 세부를 확인·조율**한다 | 오퍼·즉석 결정·제안을 담을 영어 도구가 **하나도 없다**(`will`·진행형 = E2 / `can` = 12 / `want to`·`have to` = 13 / `Let's`·`Let me`·`Why don't we` = 14) |
> | **12~15** | 확인·조율 **+ 제안·부탁·허락·의향**. 제안이 회화의 축이 되어도 된다 | 12에서 `can`이 부탁·허락을, 14에서 `Let's`·`Why don't we`가 제안을 감당한다. **유닛 14는 제안 장면이 없으면 오히려 결함**이다 |
>
> 위 표의 「회화 장면」 칸이 **"정한다 · 제안한다 · 잡는다 · 돌려준다 · 지금 하겠다고 말한다"** 로 읽히는데 유닛 번호가 **11 이하**라면 **그 칸이 틀린 것**이다.
> 저자가 노력해서 메울 수 있는 간격이 아니라 **장면 설계로만 풀리는 문제**다 — 도구 없는 저자는 단순현재로 때우게 되고, 그러면 **틀린 영어**가 학습자에게 나간다(2026-09-22 치명 5건 · §3-5).

### 3-2. 왜 이 순서인가

세 덩어리로 끊는다. 각 덩어리의 끝이 5의 배수이고, 그 자리에서 복습 블록이 앞을 묶는다.

| 덩어리 | 유닛 | 학습자가 얻는 것 |
|---|---|---|
| **① 문장을 세운다** | 1~5 | 동사 하나 · 묻고 답하기 · 의문사 · **어순**. "말이 되는 한 문장"이 나온다 |
| **② 문장을 채운다** | 6~10 | 명사·관사·대명사·있다·전치사. 부품이 제자리에 앉는다 |
| **③ 문장에 뜻을 싣는다** | 11~15 | 꾸밈 · 할 수 있다 · 하고 싶다 · 제안 · **끝맺기**. 하고 싶은 말이 한 문장으로 끝난다 |

- **어순(5)을 앞 덩어리 끝에 둔 이유**: 어순은 규칙 하나를 배우는 게 아니라 **1~4에서 본 것을 관통하는 원리**다. 먼저 놓으면 예가 없어 추상적이고, 뒤로 미루면 6~10을 어순 없이 배운다.
- **명사·관사(6·7)를 동사보다 뒤에 둔 이유**: 학교 문법서의 순서(명사 → 관사 → 대명사 → 동사)를 따르지 않는다. 말이 안 나오는 사람에게 급한 것은 **동사**이고, 관사는 말이 나온 뒤에 다듬는 것이다. 이 뒤집기 자체가 "다시 세우기"다.
- **can(12) → to(13) → 제안(14) 순서**: 셋 다 "동사 앞에 뭔가를 얹는" 같은 모양이라 붙여 둔다. 12에서 **뒤에 원형이 온다**를 세워 두면 13의 `want to + 원형`, 14의 `Let me + 원형`이 새 규칙이 아니라 같은 규칙의 반복이 된다.
- **끝맺기(15)를 마지막에 둔 이유**: `I think ~` 는 **앞 14유닛에서 만든 문장을 통째로 목적어 자리에 넣는** 틀이다. 재료가 다 있어야 성립한다.

### 3-3. 유닛별 상세

> 각 유닛의 **문법 `name` / `name_ko`** 는 그대로 시드에 넣을 수 있게 적었다(`../설계/06 §11-8` 형식).
> **학교 / 실제** 두 줄이 `explanation`(최대 500자)의 뼈대다 — 이 대비가 빠지면 E1이 아니다.
> 표현·어휘의 괄호 안 예시는 **후보**이지 확정 목록이 아니다(§11-5 — 영어 표현은 사전 확정이 필요 없다). **§4의 장부와 겹치면 버린다.**
>
> ⚠️ **문법 `name`에 품사 용어를 쓸 때는 그 말이 정말 그 품사인지 확인한다.** 이 코스는 *"학교에서 잘못 배운 용어를 바로잡는다"* 를 정체성으로 내걸었다 —
> 여기서 새 틀린 용어를 심으면 코스가 스스로를 반증한다. 실제로 그 사고가 한 번 났다(아래 유닛 4 ⚠️).
> 문법 `name`은 **복습 블록(5·10·15 정리 스텝)의 칩에 그대로 다시 뜬다** — 한 번 틀리면 두 화면에서 보인다.
>
> ⚠️ **`name_ko`는 학습자가 읽는 한국어 부제다 — 영어를 넣지 않는다.**
> 이 값은 **문법 제목 바로 밑**(`.step-subtitle`)과 **자료실 문법 목록**에 그대로 보인다. `an hour, a university` 처럼 영어 예시를 부제 자리에 넣으면
> 학습자는 제목 밑에서 **설명이 아니라 문제를 하나 더** 본다. 영어 예시는 `explanation` 본문과 `grammar_example`이 이미 들고 있다.
> 유닛 1~5는 15개 전부 한국어 부제인데 **6~10에서만 4건이 갈렸다** — 이 기획서 §3-3이 그 값을 그대로 지시했기 때문이다(2026-09-22 검수 · 확정값은 **§3-5-2**).
> **부제는 ① 한국어로, ② 제목을 되풀이하지 않고, ③ 바로 밑 설명이 반박하지 않는 프레임으로** 쓴다(§8-3 17·18).

---

#### 유닛 1·2 — 문법 설명 소급 보강 (2026-09-21 판정 · 커리큘럼은 그대로)

**판정: 소급 보강한다.** 대상은 **9002 · 9003 · 9004 세 개의 `explanation`뿐**이다.

**왜 뒤집었나** — 이 문서는 "1·2유닛은 다시 설계하지 않는다"고 적었지만, 그 문장이 막으려던 것은 *커리큘럼 재설계*(무엇을 가르칠지 다시 고르기)였다. 지금 고치려는 것은 **같은 내용의 서술 톤**이고, 근거는 셋이다.

1. **AC-10은 "문법 설명마다"** 다. 13개 중 3개가 빠진 상태로 두면 인수 조건을 "대체로 지켰다"로 읽게 되고, 그 전례가 유닛 6~15에 그대로 번진다.
2. **빠진 3개가 하필 학습자가 가장 먼저 보는 자리**다. 유닛 1·2는 코스의 첫인상이고 이탈이 가장 큰 구간인데, 코스가 내건 약속("학교에서 이렇게 배웠죠? 실제로는 이렇게 씁니다")이 **유닛 3에 가서야 처음 증명된다.** 광고와 제품이 두 유닛 동안 어긋나 있는 셈이다.
3. **비용이 거의 없다.** 시드 문자열 3개 교체이고 id·`name`·`name_ko`·예문·매핑을 건드리지 않는다 → 진도·복습 칩·자료실·테스트 어디에도 파급이 없다.

**지킬 선**

- **`name` · `name_ko` · id · 예문 · 어휘 · 표현은 손대지 않는다.** 설명 본문만 바꾼다.
- 길이는 유닛 3~5와 같은 대역(**180~260자**)으로 맞춘다. 지금 147~174자라 유닛 3~5(181~258자)와 눈에 띄게 갈린다.
- **유닛 3의 설명과 같은 문장을 쓰지 않는다.** 9013이 이미 `학교에서는 don't와 not을 같은 칸에서 배웠습니다` / `I don't be busy라는 문장이 없는 이유`를 쓰고 있다 — 아래 초안은 그 두 문장을 일부러 피했다(유닛 2는 *예고*, 유닛 3이 *세우는 자리*다).
- **backend-dev가 유닛 4·5 수정과 같은 커밋에서** 반영한다. 세 건 다 "기획서가 틀려서 생긴 것"이라 따로 낼 이유가 없다.

**초안** (backend-dev는 그대로 옮겨도 된다. ⚠️ SQL에 넣을 때 `don't` → `don''t`)

| id | `name` (그대로) | 새 `explanation` |
|---|---|---|
| **9002** | 일반동사 현재형과 3인칭 -s | 학교에서는 인칭·수 변화표를 칸마다 외웠습니다. 실제로 기억할 것은 표가 아니라 한 줄입니다 — 평소에 늘 하는 일은 동사를 그대로 쓰고, 주어가 he·she·it일 때만 끝에 -s를 붙입니다: I work, they live, he works. 시험지에서는 표를 다 채웠는데 말할 때 -s가 빠지는 이유는 한국어에 주어를 따라 동사가 바뀌는 자리가 없어서입니다. 문장을 시작하기 전에 주어가 한 사람·한 개인지만 확인하면 끝납니다. |
| **9003** | do / does 의문문 | 학교에서는 "의문문은 주어와 동사의 자리를 바꾼다"로 배웠습니다. 실제로 일반동사 문장은 자리를 바꾸지 않습니다. 앞에 Do를 하나 세우고 나머지는 그대로 둡니다: You work here. → Do you work here? 주어가 he·she·it이면 Does이고, 이때 뒤의 동사는 -s를 떼고 원래 모양으로 돌아갑니다: Does she work here? -s를 Does가 이미 가져갔기 때문입니다. 한 문장에 -s는 한 번뿐입니다. |
| **9004** | 부정문 don't / doesn't | 학교에서는 부정문 만드는 법을 문장 종류마다 따로 외웠습니다. 실제로 갈리는 지점은 하나뿐입니다 — 동사가 be동사인가 아닌가. 일반동사는 앞에 don't를 넣습니다: I don't drink coffee. 주어가 he·she·it이면 doesn't이고 뒤의 동사는 원래 모양으로 돌아갑니다: He doesn't drink coffee. -s는 doesn't가 이미 가져갔습니다. be동사는 do를 데려오지 않고 뒤에 not만 붙입니다: I am not busy. |

> **9001(be동사 현재형)은 그대로 둔다.** 대비가 문장 중간("학교에서 표로 외웠는데도 말할 때 틀리는 이유는 …")에 이미 들어 있다.
> 앞으로 쓰는 설명은 **첫 문장에서 학교를 호명**하는 편이 낫다 — 유닛 3~5가 전부 그 모양이고, 검수할 때 첫 줄만 봐도 판정이 된다.
>
> 9003의 *"주어와 동사의 자리를 바꾼다"* 와 유닛 3 9011의 *"주어와 be동사의 자리를 바꾼다"* 가 닮은 것은 **의도한 짝**이다 —
> 유닛 2에서 "일반동사는 자리를 안 바꾼다(Do를 세운다)"를 말해 두어야 유닛 3의 "be동사는 be 하나만 앞으로 나간다"가 대비로 선다. **한쪽을 지우지 말 것.**

---

#### 유닛 3 — 예·아니오는 되받아서 답한다

**문법 3개**

1. `be동사 의문문 (Am / Is / Are ~?)` / `~인가요? 라고 묻기`
   - **학교**: "주어와 be동사의 자리를 바꾼다"를 규칙으로 외웠다.
   - **실제**: 앞으로 나가는 건 **be동사 하나뿐**이다. `Do you are ~?` 처럼 do를 같이 세우는 실수가 여기서 나온다 — 유닛 1의 "동사는 하나"가 의문문에서도 그대로다.
2. `짧은 대답 (Yes, I am. / No, she doesn't.)` / `물어본 동사로 되받기`
   - **학교**: `Yes, I am.` 을 통째로 외웠다.
   - **실제**: **질문에 쓰인 동사를 그대로 되받는 것**이다. be로 물으면 be로, do로 물으면 do로 답한다. 그래서 `Do you work here?` 에 `Yes, I am.` 은 어긋난다.
3. `be동사 부정 (am not / isn't / aren't)` / `~가 아니에요`
   - **학교**: don't와 not을 같은 칸에서 배웠다.
   - **실제**: be동사는 **not만 뒤에 붙인다.** `I don't be busy`가 없는 이유다(유닛 2에서 한 줄로 예고한 것을 여기서 세운다).

**회화** — 엘리베이터에서 마주친 다른 팀 사람이 "혹시 새로 오신 분인가요?"라고 묻고, 서로 팀과 자리를 확인한다. `Are you ~?` · `Yes, I am.` · `No, I'm not.` 이 실제로 오간다.
**표현 6~10** — **되묻고 확신을 눅이는 짧은 한 마디**(예: `not really` `I guess so` `by the way` `you know what` `for sure`). 대답을 통째로 외워 바로 쓰는 성격.
**어휘 15~20** — **사람·소개·신분**(예: name, office, manager, colleague, guest, introduce, meet, friendly, sure, actually, both, also).

---

#### 유닛 4 — 무엇을·어디서·언제를 앞에 세운다

**문법 3개**

1. `의문사 의문문 (what / where / when / who)` / `무엇을·어디서·언제 묻기`
   - **학교**: 의문사를 목록으로 외우고 예문을 따로 배웠다.
   - **실제**: 새 규칙이 아니라 **유닛 2·3의 의문문 앞에 단어 하나를 더 세우는 것**이다. `Do you eat?` → `Where do you eat?` 뒤는 하나도 안 바뀐다.
2. `how + 한 마디 더 (how much / how long / how often)` / `얼마나 ~인지 묻기`
   - **학교**: how는 "어떻게"로 배웠다.
   - **실제**: how는 **혼자보다 뒤에 말을 붙여 쓰는 경우가 훨씬 많다.** 값·시간·횟수·거리를 묻는 말이 전부 여기서 나온다.
   > ⚠️ **2026-09-21 정정 — 원래 `how + 형용사`라고 적었고 시드가 그대로 받았다. 틀린 용어다.**
   > 셋 중 형용사는 `long` 하나뿐이다 — **`often`은 부사**이고 `much`는 여기서 양을 묻는 말(한정사·부사 쪽)이다.
   > **학교에서 잘못 배운 용어를 바로잡겠다는 코스가 새 틀린 용어를 심는 것**이라 치명이다. 게다가 이 `name`은 **유닛 5 복습 칩에 그대로 다시 뜬다.**
   > **확정값은 `how + 한 마디 더`** 이고, 이 코스가 품사 이름을 외우게 하는 코스가 아니라는 점에서 오히려 톤에 맞는다.
   > 설명 본문은 이미 "how 뒤에 붙은 말이 무엇을 묻는지 정한다"고 쓰여 있어 고칠 것이 없다 — **`name` 한 줄만 바꾼다.**

3. `who가 주어일 때는 어순이 바뀌지 않는다` / `누가 ~해요?`
   - **학교**: "의문문은 do를 세운다"만 배웠다.
   - **실제**: 의문사가 **주어 자리에 있으면** do를 세우지 않는다. `Who knows him?`이지 `Who does know him?`이 아니다.

**회화** — 점심시간, **평소 어디서 먹는지·얼마나 걸리는지를 서로 묻는** 대화. `Where do you usually eat?` `How long does it take?` 가 오간다.
> ⚠️ **2026-09-22 정정 — 원래 지시가 *"어디서 먹을지 정하는 대화"* 였다.** 오늘 갈 곳을 *정하는* 장면은 유닛 4에 성립하지 않는다(§3-4 ③).
> 시드(대사 9033~9038)는 다행히 **습관을 묻는 모양**으로 쓰여 고칠 것이 없다 — **지시만 틀려 있었다.** 지시를 고쳐 다음 저자가 같은 함정에 빠지지 않게 한다.
**표현 6~10** — **묻고 가늠하는 덩어리**(예: `take a look` `be about to` `on foot` `how about` `stop by`). 질문 위에 얹어 쓰는 성격.
**어휘 15~20** — **식사·거리·시간**(예: lunch, restaurant, menu, price, minute, order, walk, far, cheap, expensive, close, wait).

---

#### 유닛 5 — 순서가 곧 조사다 ★복습(1~5)

**문법 3개**

1. `기본 어순 (주어 + 동사 + 목적어)` / `자리가 뜻을 정한다`
   - **학교**: 1~5형식 표로 배웠다.
   - **실제**: 한국어는 **조사**가 역할을 알려 주지만 영어는 **자리**가 알려 준다. `The manager knows Mina.`와 `Mina knows the manager.`는 **쓰인 단어가 하나도 안 바뀌고 자리만 다른데 뜻이 뒤집힌다.** 형식을 외울 게 아니라 자리를 지키면 된다.
   > ⚠️ **2026-09-21 정정 — 원래 제안한 `I love you` / `You love me`도, 시드가 쓴 `I know her.` / `She knows me.`도 같은 흠이 있다. 둘 다 쓰지 않는다.**
   > **어순을 보이는 예시에 대명사 쌍(I·me / he·him / she·her)을 쓰지 않는다.** 대명사는 자리에 따라 **모양이 바뀌고**(I → me), 그 규칙은 **유닛 8에서 가르칠 내용**이다.
   > 유닛 5에서 "쓰인 단어는 같고 자리만 다르다"고 말해 놓고 3유닛 뒤에 "자리가 바뀌면 모양도 바뀐다"를 가르치면 **학습자가 본 유일한 예시가 정면으로 뒤집힌다.**
   > **대신 3인칭 이름·명사 두 개**를 쓴다. 동사의 -s까지 그대로 남아(`knows`) **변수가 자리 하나로 줄어든다** — 이 문법이 보여주려는 것만 남는다.
   > 문법 예문(9061·9062)도 같은 이유로 `I know your manager.` / `Your manager knows me.` 를 쓰면 안 된다. **이름 두 개짜리 쌍**으로 바꾼다(예: `Ben helps Mina.` / `Mina helps Ben.`).

2. `장소 먼저, 시간 나중` / `뒤에 붙이는 말의 순서`
   - **학교**: 배운 적이 거의 없다.
   - **실제**: `I work at home in the morning.` 순서를 뒤집으면 문법이 틀린 건 아니어도 어색하다.
3. `주어를 빼지 않는다 — 날씨·시간의 it` / `주어 없는 문장은 없다`
   - **학교**: "비인칭 주어 it"이라는 용어로 배웠다.
   - **실제**: 한국어는 "비 와"로 끝나지만 영어는 **주어가 없으면 문장이 아니다.** 뜻이 없어도 it을 세운다.

**회화** — 퇴근길 통화. **이미 만나기로 한 약속**의 장소·시간을 서로 확인한다(약속을 *새로 잡는* 장면이 아니다 — §3-4). 주어 자리를 채우는 it(`It's cold outside.` `It takes five minutes.`)과 "장소 먼저, 시간 나중"이 대사 안에서 드러난다.
> ⚠️ **2026-09-21 정정 두 가지.**
> ① 원래 `It's raining`을 예로 들었는데 **진행형은 E2**다(§2 경계). E1 안에서는 `It rains a lot here.` · `It's cold outside.` 로 쓴다.
> ② 원래 회화 지시가 *"어디서 만날지를 정한다"* 였다 — **E1 유닛 5에는 제안을 담을 말이 없다**(§3-4). 제안을 주고받는 장면은 유닛 12(can) 이후로 미루고,
> 여기서는 **이미 정해진 약속의 세부를 확인**한다. 현재 시드의 `Where do we meet?` 가 어색하게 읽히는 것은 저자의 실수가 아니라 **이 지시가 경계 밖을 요구했기 때문**이다.
> 시드 한 줄 권고(판단은 backend-dev): 3번째 대사를 `I know. I get off at the next stop. We meet at six, right?` 로 두면 4번째 `Where do we meet?`(시간은 정해졌고 장소만 남았다)가 자연스러워진다.

**표현 6~10** — **자리가 굳어 뒤집을 수 없는 덩어리**(예: `pick up` `drop by` `on time` `get off` `head home`).
**어휘 15~20** — **이동·장소**(예: station, bus, subway, street, corner, exit, arrive, leave, drive, outside, almost, together는 제외 — §4 장부 확인).

> ⚠️ **유닛 5의 정리 스텝에는 1~5 복습 블록이 붙는다.** 유닛 1~5가 **모두 존재하는 상태**로 배포돼야 한다(§7 예외 상황 E-3).

---

#### 유닛 6 — 셀 수 있는지부터 본다

**문법 3개**

1. `셀 수 있는 명사와 셀 수 없는 명사` / `a를 붙일 수 있나부터 본다`
   - **학교**: 물질명사·추상명사라는 **용어**로 나눴다.
   - **실제**: 질문은 두 개뿐이다 — **a를 붙일 수 있나, 복수로 만들 수 있나.** water·advice·information에 -s를 못 붙이는 이유가 여기서 끝난다.
2. `복수형 -s` / `둘 이상이면 반드시 표시한다`
   - **학교**: -s / -es / -ies 변화표를 외웠다.
   - **실제**: 한국어는 "사과 세 개"처럼 수를 말해도 명사가 그대로라 **-s를 빼먹는다.** 규칙보다 "둘 이상이면 무조건"이 먼저다.
3. `a와 an은 철자가 아니라 소리로 고른다` / `첫소리로 고른다`  ← **2026-09-22 정정**(원래 `an hour, a university` — 부제에 영어 예시를 넣지 않는다 · §3-5-2)
   - **학교**: "모음 앞에는 an"으로 외웠다.
   - **실제**: 기준은 **첫소리**다. hour는 h로 쓰지만 an, university는 u로 쓰지만 a다.

**회화** — 퇴근길 장보기. **살 것은 이미 목록에 적혀 있고**, 그 목록을 읽으며 **수량을 확인**한다. 셀 수 있는 것과 없는 것이 한 장면에 섞여 나온다.
> ⚠️ **2026-09-22 정정 — 원래 지시가 *"무엇을 살지 주고받는다"* 였다.** 무엇을 살지 *정하는* 장면은 유닛 6에 성립하지 않는다(§3-4 ③).
> 이 지시 때문에 대사 9061에 **`I carry the water, then.` / "그럼 물은 제가 들게요"** 라는 **틀린 대응**이 들어갔다 — 영어 단순현재는 오퍼가 아니다.
> **누가 무엇을 드는지는 "늘 그렇게 한다"는 역할 서술로** 말한다(§3-4 ⑤). 수정값은 **§3-5-1**.
**표현 6~10** — **수량을 말하는 덩어리**(예: `a lot of` `a little` `a few` `run out of` `some more`).
**어휘 15~20** — **음식·장보기**(예: water, bread, rice, bottle, bag, money, shop, buy, sell, cook, fresh, heavy).

---

#### 유닛 7 — 처음엔 a, 다시 말하면 the

**문법 3개**

1. `a와 the의 갈림` / `듣는 사람이 아는가로 정한다`
   - **학교**: "a는 하나, the는 그"로 외웠다.
   - **실제**: 기준은 개수가 아니라 **듣는 사람이 무엇인지 아느냐**다. 처음 꺼낸 것은 a, 이미 나왔거나 서로 아는 것은 the.
2. `관사를 아예 쓰지 않는 자리` / `식사·교통수단·집에는 붙지 않는다`  ← **2026-09-22 정정**(원래 `have lunch, by bus, go home` · §3-5-2)
   - **학교**: 거의 다루지 않았다.
   - **실제**: 식사·교통수단·장소의 기능(at school, go to bed)에는 **관사가 없다.** 여기에 a나 the를 넣으면 바로 어색해진다.
3. `the가 통째로 굳은 자리` / `따질 대상이 아니라 외울 대상`  ← **2026-09-22 정정**(원래 `in the morning, on the phone` · §3-5-2)
   - **학교**: 규칙으로 설명하려 했다.
   - **실제**: 규칙이 아니라 **덩어리로 굳은 것**이다. 외울 대상이지 따질 대상이 아니다.

**회화** — 자리를 비운 사이 온 택배가 **지금 어디 있는지 확인**하는 대화. `a package` → `the package`로 바뀌는 흐름이 그대로 보인다.
> ⚠️ **2026-09-22 정정 — 받는 사람이 *"언제 열지·무엇부터 읽을지"를 즉석에서 정하지 않는다.*** 그 뜻을 담을 말이 유닛 7에 없다(§3-4 ③).
> 이 지시가 비어 있어 대사 9072·9074에 **`I open it after lunch.` / "점심 먹고 열어 볼게요"**, **`I read the letter first.` / "편지부터 읽을게요"** 가 들어갔다.
> **물건이 지금 어떤 상태인지**(`The package is fine there until lunch.`)나 **평소 습관**(`I always read letters first.`)으로 말한다. 수정값은 **§3-5-1**.
**표현 6~10** — **관사가 포함된 채로 굳은 덩어리**(예: `in the morning` `at the moment` `on the phone` `all the time`). ⚠️ **`by the way`는 유닛 3에서 이미 썼다 — 쓸 수 없다**(§4 장부).
**어휘 15~20** — **사무·물건**(예: package, box, letter, key, phone, table, chair, send, receive, find, empty, heavy는 유닛 6과 중복 확인).

---

#### 유닛 8 — 자리가 정해 주는 말: I · my · me

**문법 3개**

1. `인칭대명사의 네 모양 (I / my / me / mine)` / `어디에 앉느냐가 모양을 정한다`
   - **학교**: 표를 가로로 외웠다(I-my-me-mine).
   - **실제**: 외울 표가 아니라 **자리 규칙**이다. 주어 자리면 I, 동사·전치사 뒤면 me. 유닛 5의 어순이 그대로 이어진다.
2. `it과 they로 다시 받기` / `한 번 말한 것은 대명사로 받는다`
   - **학교**: "대명사는 명사를 대신한다"로 배웠다.
   - **실제**: 한국어는 주어·목적어를 **생략**하지만 영어는 반드시 받는다. "샀어? 좋았어?"가 `Did you buy it? Was it good?`이 되는 자리(과거형은 E2, 여기서는 현재형으로).
3. `this / that / these / those` / `거리로만 고르는 말이 아니다`  ← **2026-09-22 정정**(원래 `가까운 것과 먼 것` · §3-5-2)
   - **학교**: 거리로만 배웠다.
   - **실제**: 전화·소개에서 굳어 쓰는 `This is ~` 처럼 **거리와 무관한 쓰임**이 일상에서 더 잦다.
   > ⚠️ **부제가 설명에 반박당하면 안 된다.** 원래 부제 `가까운 것과 먼 것`은 **학교 버전**인데, 바로 밑 설명이 *"실제로는 거리와 무관한 자리가 더 잦다"* 고 뒤집는다.
   > 화면에서 **제일 큰 글씨 바로 밑**에 틀린 요약이 남는 셈이다. **부제는 그 유닛이 도달시키려는 쪽(실제 쓰임)을 적는다**(§8-3 18).

**회화** — 책상에 섞인 물건이 **누구 것인지 확인하는** 대화. `Is that yours?` `It's mine.` 주인이 밝혀지면 **그 물건이 지금 어디 있는지·원래 어디 두는지**를 현재 상태로 말하고 끝낸다.
> ⚠️ **2026-09-22 정정 — 원래 지시가 *"확인하고 돌려주는"* 이었다.** **돌려주겠다고 나서는 대사(오퍼)를 넣지 않는다** — 유닛 8에 오퍼를 담을 말이 없다(§3-4 ③).
> 이 지시 때문에 대사 9083에 **`Then I leave it on his desk.` / "그럼 준 씨 자리에 둘게요"** 가 들어갔다.
> **주인이 누구인지를 소유격·소유대명사로 확정하는 것**(`Then it is Jun's, not yours.`)이 이 유닛이 보여줘야 할 문법이고, 그것으로 장면이 닫힌다. 수정값은 **§3-5-1**.
**표현 6~10** — **소유·양보의 한 마디**(예: `help yourself` `by myself` `on your own` `all yours` `take care of`).
**어휘 15~20** — **소지품·관계**(예: wallet, umbrella, charger, own, borrow, lend, share, keep, each, other, careful).

---

#### 유닛 9 — 있다고 말할 땐 There로 연다

**문법 3개**

1. `There is / There are` / `~가 있어요`
   - **학교**: "There is = ~가 있다"로 외웠다.
   - **실제**: **have와 갈리는 자리**다. "방에 의자가 있다"를 `The room has a chair`라고 하지 않는다. 무엇이 있는지를 말할 때는 There로 연다.
2. `There is의 의문·부정` / `~가 있나요? · 없어요`
   - **학교**: 문장 하나만 배우고 넘어갔다.
   - **실제**: 유닛 3의 be동사 규칙이 그대로다 — `Is there ~?` · `There isn't ~`. 그리고 **뒤에 오는 명사의 수**로 is/are를 고른다(유닛 6과 연결).
3. `위치는 문장 끝에 붙인다` / `무엇이 있는지를 먼저 말한다`  ← **2026-09-22 정정**(원래 `There is a cafe on the first floor.` · §3-5-2)
   - **학교**: 전치사구를 따로 배웠다.
   - **실제**: 유닛 5의 "장소 먼저, 시간 나중"이 여기서도 그대로다. 장소구는 통째로 **뒤에** 간다.

**회화** — 새로 온 사람에게 사무실 층을 안내한다. `Is there a printer on this floor?` 가 오간다.
**표현 6~10** — **위치를 가리키는 덩어리**(예: `next to` `across from` `in front of` `over there` `right here`).
**어휘 15~20** — **건물·시설**(예: cafe, printer, elevator, restroom, parking, wall, window, upstairs, downstairs, inside, quiet).

---

#### 유닛 10 — 시간과 장소에 붙는 at · on · in ★복습(6~10)

**문법 3개**

1. `시간의 at / on / in` / `시각·날짜·달의 크기 순서`
   - **학교**: 셋을 따로따로 외웠다.
   - **실제**: **크기 순서 하나**다. 시각 at → 날짜·요일 on → 달·계절·연도 in. 점에서 면으로 넓어진다.
2. `장소의 at / on / in` / `같은 크기 규칙이 장소에도 간다`
   - **학교**: 시간과 장소를 다른 규칙으로 배웠다.
   - **실제**: 같은 규칙이다. 지점 at the door → 면 on the table → 공간 in the room.
3. `빈도부사가 앉는 자리` / `be동사 뒤, 일반동사 앞`
   - **학교**: 빈도부사 목록을 외웠다.
   - **실제**: 뜻보다 **자리**가 문제다. `I am always busy` / `I always work late` — be 뒤, 일반동사 앞.

**회화** — **다음 주 회의는 이미 금요일 10시로 잡혀 있고**, 그 시각에 **올 수 있는지·어디서 하는지를 확인**한다. 시각(at)·요일(on)·시간대(in)가 **첫 줄에 함께** 나오고, 장소의 at/on/in과 빈도부사가 뒤를 받는다. 한쪽은 가능한 시간대를 **상태로** 말한다(`I am always busy after lunch.`).
> ⚠️ **2026-09-22 정정 — 원래 지시가 *"다음 주 약속을 잡으며 요일과 시각을 조율한다"* 였다.** **약속을 잡는 장면은 유닛 10에 성립하지 않는다**(§3-4 ③).
> 이 지시 때문에 시드 회화가 **`Are you free on Friday?` → `Then at ten?` → `Ten is good.` 이라는 정확한 제안→합의 구조**가 됐고, 합의를 표시할 말이 없어
> 대사 9108이 **`I put it in my schedule now.` / "지금 일정에 넣을게요"** 로 때워졌다.
> **첫 대사에서 "회의가 금요일 10시에 있다"는 사실을 먼저 세우고**, 일정이 잡혔다는 것은 **상태로** 말한다(`It is already in my schedule.`). 수정값은 **§3-5-1**(이 유닛은 **3줄을 한꺼번에** 고쳐야 뜻이 통한다).
**표현 6~10** — **일정·약속 덩어리**(예: `in advance` `put off` `make it` `show up` `first thing`). ⚠️ **`on time`은 유닛 5에서 이미 썼다 — 쓸 수 없다**(§4 장부).
**어휘 15~20** — **날짜·일정**(예: day, week, month, hour, birthday, holiday, schedule, appointment, always, sometimes, ready, soon, tonight).
> ⚠️ `usually` `often` `never` `during`은 **유닛 1·2에서 이미 썼다.** 문법 설명·예문에는 얼마든지 나와도 되지만 **어휘 표제어로 다시 올리지 않는다.**

---

#### 유닛 11 — 꾸미는 말은 앉을 자리가 있다

**문법 3개**

1. `형용사의 두 자리` / `명사 앞이거나 be동사 뒤거나`
   - **학교**: "형용사는 명사를 꾸민다"만 배웠다.
   - **실제**: 자리가 **둘**이다. `a busy day`(명사 앞)와 `I am busy`(be 뒤). 어느 자리에 앉히느냐로 문장 모양이 달라진다.
2. `부사는 -ly, 그런데 예외가 있다` / `fast · hard · late`
   - **학교**: "형용사 + ly = 부사"로 외웠다.
   - **실제**: 모양이 그대로인 것(fast, hard, late)이 있고, **hardly는 뜻이 아예 다르다**(거의 ~않다). ly를 붙이면 되는 줄 알고 틀리는 자리다.
3. `very / really / too / so` / `too는 "매우"가 아니다`
   - **학교**: too를 "또한 · 너무"로 배웠다.
   - **실제**: too는 **지나치다**는 부정적 판단이다. `too cold`는 "아주 춥다"가 아니라 "추워서 곤란하다"에 가깝다.

**회화** — 사무실 온도·컨디션을 두고 짧게 주고받는다. `It's too cold in here.`
**표현 6~10** — **정도를 눅이는 덩어리**(예: `a little bit` `more or less` `pretty much` `not at all` `at least`). `kind of`는 유닛 2에서 썼다.
**어휘 15~20** — **감정·상태·정도**(예: cold, hot, hungry, happy, angry, quiet, loud, slowly, quickly, enough, almost, quite).

---

#### 유닛 12 — 할 수 있다와 해 주세요는 같은 단어

**문법 3개**

1. `can / can't` / `할 수 있어요 · 못 해요`
   - **학교**: "can = ~할 수 있다"로 외웠다.
   - **실제**: 중요한 건 뜻이 아니라 **뒤에 오는 모양**이다. can 뒤는 언제나 **원형** — `He can swims` `can to swim`이 안 되는 이유. 유닛 1의 "-s는 하나만"과 같은 규칙이다.
2. `Can you ~? / Can I ~?` / `부탁과 허락`
   - **학교**: "능력"으로만 배웠다.
   - **실제**: 같은 can이 상황에 따라 **부탁**(Can you ~?)과 **허락 구하기**(Can I ~?)가 된다. 능력으로만 알면 부탁을 못 한다.
3. `Could you ~? 는 과거가 아니다` / `더 정중한 지금의 부탁`
   - **학교**: could를 can의 과거로 배웠다.
   - **실제**: 부탁에 쓰는 could는 **지금 이야기**이고 더 정중할 뿐이다.
   > ⚠️ 여기서 **과거 시제를 설명하지 않는다.** "과거형처럼 생겼지만 지금 이야기"라고만 말하고 멈춘다 — 과거는 E2다(§2).

**회화** — 자료 출력을 부탁하고 응하는 대화. `Can you ~?` `Sure, no problem.`
**표현 6~10** — **부탁·수락·거절 덩어리**(예: `no problem` `hold on` `never mind` `go ahead` `give a hand`).
**어휘 15~20** — **도움·업무 동작**(예: help, check, print, carry, open, close, answer, again, maybe, please, quickly는 유닛 11과 중복 확인).

---

#### 유닛 13 — 동사를 두 개 쓰고 싶을 때

**문법 3개**

1. `want to / need to + 동사원형` / `동사 사이에 to를 넣는다`
   - **학교**: "to부정사의 명사적 용법"이라는 **용어**부터 배웠다.
   - **실제**: 요령은 하나다 — **동사를 두 개 이어 쓰고 싶으면 사이에 to를 넣는다.** `I want go`가 안 되는 이유가 이걸로 끝난다.
2. `like / enjoy 뒤에는 -ing` / `to가 아니라 -ing가 오는 동사`
   - **학교**: "동명사를 목적어로 취하는 동사" 목록을 외웠다.
   - **실제**: 목록을 다 외울 일이 아니다. **자주 쓰는 몇 개**(enjoy, finish, keep, mind)만 -ing이고 나머지는 대개 to다.
3. `have to` / `해야 해요`
   - **학교**: "must = ~해야 한다"로 배웠다.
   - **실제**: 일상에서 압도적으로 쓰는 것은 **have to**다. 그리고 유닛 1의 -s 규칙이 그대로 붙는다 — `He has to ~`.

**회화** — 퇴근 전, 오늘 꼭 끝내야 할 일을 두고 주고받는다. `I have to finish this today.`
**표현 6~10** — **할 일·마감 덩어리**(예: `work on` `get it done` `catch up` `be done with` `figure out`).
**어휘 15~20** — **업무·마감**(예: report, deadline, project, task, finish, continue, decide, hope, forget, later, almost는 유닛 11과 중복 확인).

---

#### 유닛 14 — 시키는 말이 무례한 건 아니다

**문법 3개**

1. `명령문 — 주어를 빼고 동사로 시작한다` / `이렇게 하세요`
   - **학교**: "명령문 = 명령", "공손하려면 please를 붙인다"로 배웠다.
   - **실제**: 안내·설명에서 가장 많이 쓴다 — `Take the elevator.` `Turn left.` **무례한 말이 아니다.** 유닛 5의 "주어를 빼지 않는다"에서 **유일하게 빼는 자리**이기도 하다.
2. `Let's ~ / Let me ~` / `같이 해요 · 제가 할게요`
   - **학교**: "Let's = ~하자"로 외웠다.
   - **실제**: `Let me check.`(제가 확인해 볼게요)처럼 **혼자 하는 말**에도 쓴다. 뒤는 언제나 원형(유닛 12와 같은 규칙).
3. `Why don't we ~? / How about -ing?` / `제안을 부드럽게`
   - **학교**: "Why don't you ~?"를 이유를 묻는 말로 읽었다.
   - **실제**: 이유를 묻는 게 아니라 **제안**이다. how about 뒤는 -ing(유닛 13과 연결).

**회화** — 문제를 앞에 두고 어떻게 할지 제안하고 정한다. `Let's ~` `Why don't we ~?`
**표현 6~10** — **제안·동의 덩어리**(예: `sounds good` `why not` `go for it` `let's say` `come along`).
**어휘 15~20** — **방향·동작 지시**(예: turn, follow, push, pull, press, hold, left, right, straight, first, then, next).

---

#### 유닛 15 — 하고 싶은 말을 한 문장으로 끝낸다 ★복습(11~15)

**문법 2개** (계약은 2~3 — 마지막 유닛은 복습 블록이 함께 붙으므로 2개로 둔다)

1. `I think ~ / I don't think ~` / `제 생각엔 ~인 것 같아요`
   - **학교**: "that은 생략할 수 있다"로 배웠다.
   - **실제**: 말할 때는 that을 **거의 안 붙인다.** 그리고 부정은 뒤가 아니라 **앞에서** 한다 — `I think it's not good`보다 `I don't think it's good`이 자연스럽다. 한국어와 순서가 반대인 자리다.
2. `It's ~ to 동사` / `긴 말은 뒤로 미룬다`
   - **학교**: "가주어 it, 진주어 to부정사"라는 **용어**로 배웠다.
   - **실제**: 하고 싶은 말이 길어질 때 **일단 It's로 시작해 두고 뒤에 이어 붙이는 요령**이다. `It's hard to explain.` 유닛 5의 "주어 없는 문장은 없다"가 여기서 다시 쓰인다.

**회화** — 두 안을 놓고 어느 쪽이 나은지 의견을 주고받는다. `I think ~` `I don't think ~` 가 오간다.
**표현 6~10** — **말을 맺고 넘기는 덩어리**(예: `I mean` `that's it` `in my opinion` `to be honest` `either way`).
**어휘 15~20** — **의견·판단**(예: idea, opinion, reason, problem, choice, agree, mean, important, better, wrong, maybe는 유닛 12와 중복 확인).

> 이 유닛의 정리 스텝에 **11~15 복습 블록**이 붙고, 그것으로 **코스 1이 닫힌다.** 마지막 유닛을 5의 배수로 맞춘 이유가 이것이다.

---

### 3-4. E1 경계 안에서 제안·의향을 말하는 법 (**모든 유닛 저자 필독 — 장면을 정하기 전에 읽는다**)

> **왜 이 절이 생겼나**: qa가 유닛 5 회화의 `Where do we meet?` 를 어색하다고 짚었다. 원인은 저자가 아니라 **경계**다 —
> `should`는 E4, `want to`는 유닛 13, 진행형·미래는 E2, `Let's` · `Why don't we`는 유닛 14다.
> **유닛 5에서 약속을 "잡는" 장면을 쓰라고 지시하면 저자는 궁지에 몰린다.**
>
> ⚠️ **그리고 실제로 재발했다** — 이 절을 2026-09-21에 확정하면서 **§3-1·§3-3의 장면 지시는 고치지 않고 두었다.**
> 그래서 유닛 6("무엇을 살지 주고받는다") · 8("확인하고 **돌려주는**") · 10("약속을 **잡으며** 조율한다")이 그대로 남았고,
> 다음 저자는 이 절을 읽고도 **지시를 따라 같은 함정에 빠졌다**(치명 5건 — §3-5).
> **규칙을 새로 쓰면 그 규칙과 어긋난 지시를 같은 라운드에서 함께 고친다.** 안 고친 지시는 규칙을 이긴다.

#### ① 유닛 12 이전에도 쓸 수 있는 것 (전부 이미 가르친 것)

| 도구 | 언제부터 | 이렇게 쓴다 |
|---|---|---|
| **의문사 의문문** | 유닛 4 | `Where do we meet?` `What time do we start?` — **단, 약속 자체는 이미 합의된 뒤여야 자연스럽다**(아래 ③) |
| **do 의문문으로 의향 확인** | 유닛 2 | `Do you have time at six?` `Do you know the cafe near exit four?` |
| **be 의문문 + 짧은 대답** | 유닛 3 | `Are you free after work?` — `Yes, I am.` / `No, I'm not.` |
| **`how about` + 명사** | **유닛 4(표현 9031)** | `How about the cafe near exit four?` `How about six?` — **뒤에 명사만 놓으면 -ing(유닛 13·14) 없이도 제안이 된다.** E1 초반에 쓸 수 있는 **유일한 정면 제안**이다 |
| **`be about to`** | 유닛 4(표현 9033) | `I'm about to leave.` — 임박한 의향을 현재형 그대로 말한다 |
| 주어 it | 유닛 5 | `It takes twenty minutes.` `It's almost six.` — 조율의 근거를 대는 말 |
| There is / are | 유닛 9 | `There is a cafe near exit four.` — 장소를 제시한다 |
| 빈도부사·전치사 | 유닛 10 | `I'm always free on Friday.` |
| **can** | **유닛 12** | `Can you come at six?` `Can I join?` — **문법이 제안을 감당하는 첫 자리** |

> ⚠️ **`how about`은 유닛 4에서 이미 썼다.** 유닛 14의 표현 후보에 `how about -ing`를 다시 올릴 수 없다(코스 내부 중복 금지 — §4).
> 유닛 14는 **문법 스텝에서** `How about -ing?` 를 다루고, 표현 스텝은 다른 덩어리(`sounds good` · `why not` 등)로 채운다.

#### ② 이 코스에서 절대 당겨 쓰지 않는 것

`will` · 진행형(E2) · `should` · `would` · `Could you ~?`를 제외한 완곡 어법(E4) · `want to` · `have to`(유닛 13) · `Let's` · `Let me` · `Why don't we ~?` · `How about -ing?`(유닛 14).

#### ③ 장면 설계 규칙 — 경계는 **유닛 11과 12 사이**에 있다

| | **유닛 1~11** | **유닛 12~15** |
|---|---|---|
| **쓰는 장면** | **이미 정해진 일의 세부를 확인·조율한다.** 장소·시각·수량·소요 시간·누가 오는지·누구 것인지·무엇이 어디 있는지를 **의문사와 현재형**으로 묻고 답한다 | 확인·조율 **+ 제안·부탁·허락·의향.** 제안이 회화의 축이 되어도 된다 |
| **쓰지 않는 장면** | **제안 → 반대 제안 → 합의.** 구체적으로 *"무엇을 살지 정한다" · "약속을 잡는다" · "돌려주겠다고 나선다" · "지금 하겠다고 말한다" · "어디서 먹을지 정한다"* | **(없다.)** 오히려 **유닛 14에 제안 장면이 없으면 결함**이다 |
| **그 유닛이 가진 도구** | 의문사(4) · do/be 의문문(2·3) · `how about` + 명사(4) · `be about to`(4) · 주어 it(5) · There is(9) · 빈도부사·전치사(10) | **12 `can` / `Can you ~?` / `Can I ~?` / `Could you ~?`** → **13 `want to` · `need to` · `have to`** → **14 `Let's` · `Let me` · `Why don't we ~?` · `How about -ing?`** |

- **왜 11에서 끊기나**: 유닛 11까지는 오퍼·즉석 결정·합의를 담을 말이 **하나도 없다.** 유닛 12에서 `can`이 부탁과 허락을 감당하고, 13에서 `want to`·`have to`가 의향과 필요를 붙이고, **14에서 `Let's`·`Why don't we`로 제안이 완성된다.**
  유닛 14의 회화 장면(*"그럼 이렇게 해 볼까요?"*)이 이 코스에서 **제안이 처음 주인공이 되는 자리**다 — 거기서는 제안 장면이 **정상이고 필수**다.
- **저자가 노력해서 될 일이 아니다.** 장면이 오퍼를 요구하는데 도구가 없으면 저자는 **단순현재로 때울 수밖에 없다.**
  영어 단순현재는 **습관·사실**이지 즉석 결정이 아니다 — `I carry the water`는 "제가 들게요"가 아니라 "저는 (평소) 물을 나른다"다.
  그 짝이 시드에 남으면 학습자는 **`I carry the water = 제가 들게요` 라는 틀린 대응을 외운다.** 틀린 영어를 가르치는 것이라 결함 중 가장 무겁다.
- **그래서 이것은 장면 설계로 푸는 문제다.** §3-1·§3-3의 장면 지시가 "정한다/제안한다/잡는다/돌려준다"로 읽히면 **그 지시가 틀린 것**이고, 고칠 것은 문장이 아니라 지시다.
- **장면 지시를 쓸 때의 점검 한 줄**
  > *"이 장면에서 두 사람이 **무엇을 새로 정하는가**?"* 에 답이 나오는데 유닛 번호가 **11 이하**라면, 그 장면은 다시 쓴다.
  > 답이 "아무것도 정하지 않는다 — 이미 정해진 것을 맞춰 볼 뿐이다"여야 한다.

#### ④ 판정 — `Let's`를 유닛 14에서 앞으로 당기지 않는다

1. **유닛 12 → 13 → 14는 "동사 앞에 뭔가를 얹는다"는 한 덩어리**로 묶여 있다(§3-2). 유닛 12에서 *"can 뒤는 언제나 원형"* 을 세운 다음이라 `Let's + 원형` · `Let me + 원형`이 **새 규칙이 아니라 같은 규칙의 반복**이 된다. 유닛 5로 당기면 학습자는 "Let's 뒤에는 원형"을 **이유 없이 외워야** 한다 — 이 코스가 반대하는 바로 그 방식이다.
2. **유닛 5의 문법은 어순**이다. 제안 표현을 끼워 넣으면 그 유닛의 문법·회화·표현이 한 테마로 묶이지 않는다(`../설계/06 §11-5`).
3. **필요한 만큼은 이미 있다** — `how about` + 명사(유닛 4)로 제안 한 마디는 지금도 된다. 없는 것은 "제안을 주고받는 대화"이고, 그건 장면 배정으로 푸는 문제지 문법을 당겨서 풀 문제가 아니다.

> 표현 스텝은 문법 없이도 덩어리를 넣을 수 있는 자리라(`../설계/06 §11-5`) 예외적으로 앞당길 수 있다.
> 다만 **앞당긴 표현은 원래 배정된 유닛에서 빼야 한다**(코스 내부 중복 금지). 옮기면 §4 장부와 그 유닛의 §3-3 후보 목록을 **같이** 고친다.

#### ⑤ 경계 밖 뜻을 우회하는 법 — 저자가 궁지에 몰렸을 때 꺼내 쓰는 도구 (2026-09-22 신설)

장면을 ③대로 고쳤는데도 **한 줄이 경계 밖 뜻을 요구할 때**가 있다. 그 줄을 **단순현재로 때우지 말고** 아래처럼 **뜻 자체를 옮긴다.**
원칙은 하나다 — **영어를 먼저 쓰고, 그 영어가 실제로 뜻하는 바를 한국어로 적는다**(§8-3 16).

| 담고 싶은 뜻 | 쓰면 안 되는 것 | 이렇게 옮긴다 | 예 (E1 안에서 성립) |
|---|---|---|---|
| **오퍼** — "제가 할게요" | 단순현재 (`I carry the water.`) | **상태·역할 서술**로 — 누가 늘 맡는 일인지, 누구 쪽 몫인지 | `I always carry the water, then.` / 그럼 물은 늘 제가 들어요<br>`The water is on my side of the list, then.` / 그럼 물은 제 쪽 목록이네요 |
| **즉석 결정** — "지금 넣을게요" | 단순현재 + `now` (`I put it in my schedule now.`) | **이미 그렇게 되어 있다는 확인**으로 | `It is already in my schedule.` / 제 일정에 이미 들어 있어요 |
| **미래 약속** — "6시에 갈게요" | `will` · 진행형 (둘 다 **E2**) | **현재 상태 서술**로, 또는 **이미 잡힌 일의 확인**으로 | `The meeting is at six.` · `I go home by bus at six.` |
| **제안** — "그럼 10시에 할까요?" | `Let's` · `Why don't we` (**유닛 14**) · `shall`(안 가르친다) | **`how about` + 명사**(유닛 4부터 유일한 정면 제안) 또는 **되묻는 확인** | `How about ten?` · `Then ten, right?` |
| **의향 확인** — "하실래요?" | `want to`(**유닛 13**) · `would`(**E4**) | **do / be 의문문**(유닛 2·3) | `Do you have time at six?` · `Are you free in the morning?` |
| **우선순위** — "편지부터 읽을게요" | 단순현재 (`I read the letter first.`) | **습관** 또는 **중요도의 상태 서술**로 | `Then I always read letters first.` · `The letter is the urgent one.` |
| **부탁** — "이것 좀 해 주세요" | 명령문(**유닛 14**) · `can`(**유닛 12**) | 유닛 11까지는 **장면에서 뺀다.** 12부터 `Can you ~?`로 정면 처리 | (유닛 12 `Can you help me for a minute?`) |
| **과거** — "뒀어요 · 봤어요" | 과거형(**E2**) — **한국어 뜻에서도 쓰지 않는다** | **현재 상태 서술**로 | `The box is on the table.` / 상자는 탁자에 있어요 |

> ⚠️ **한국어 쪽도 경계 안이어야 한다.** 영어는 현재형인데 한국어 뜻만 과거·미래·의향으로 적으면,
> 학습자는 **E1이 아직 안 가르친 시제·서법을 한국어로 먼저 배우고**, 그 뜻을 **현재형 영어에 붙여** 외운다. 이것이 이번 결함의 정확한 모양이다.

> ✅ **유닛 6~10 검수는 2026-09-22에 끝났다.** `will` · 진행형 · `should` · `Let's` · `Why don't we` 가 **샌 곳은 없다.**
> 대신 **그 반대 방향의 결함 5건**이 나왔다 — 경계 밖 뜻을 **한국어에만** 실어 두고 영어는 단순현재로 남긴 줄들이다.
> 원인은 저자가 아니라 **이 문서의 장면 지시**였고, 지시는 위 ③대로 고쳤다. **시드 수정값은 §3-5**에 있다.

---

### 3-5. 유닛 6~10 수정 지시 (2026-09-22 · backend-dev가 그대로 옮긴다)

> **이 수정은 전부 이 기획서가 틀려서 생긴 것이다** — §3-1·§3-3의 장면 지시가 §3-4 ③과 어긋난 채 남아 있었다. **콘텐츠 작업자의 결함으로 세지 않는다.**
> 고칠 파일은 `src/main/resources/data-course-en-content.sql` 하나다. **id·매핑·개수는 하나도 바뀌지 않는다** — 문자열만 바꾼다.
> 그래서 진도·복습 칩·자료실·수량 테스트 어디에도 파급이 없다. ⚠️ SQL에 넣을 때 `don't` → `don''t`, `Jun's` → `Jun''s`.
> **유닛 11~15 작업과 같은 커밋에 섞지 않는다** — 되돌리기 쉽게 따로 낸다.

#### 3-5-1. `dialog_line` — 회화 대사 (표의 두 칸은 각각 `jp`(영어) · `meaning_ko`)

| # | id | 유닛 | 등급 | 현재 | 확정 새 값 | 왜 |
|---|---|---|---|---|---|---|
| 1 | **9061** | 6 | **치명** | `I carry the water, then. Two bottles are heavy.`<br>`그럼 물은 제가 들게요. 두 병은 무거우니까요.` | `I always carry the water, then. Two bottles are heavy.`<br>`그럼 물은 늘 제가 들어요. 두 병은 무거우니까요.` | `always` 하나로 **습관**임이 확정되고 한국어가 그 뜻과 같아진다. 오퍼의 화용은 "그럼"이 그대로 실어 나른다. `water`(무관사·단수 be) ↔ `two bottles`(복수 -s·are) 대비는 그대로 남는다 |
| 2 | **9062** | 6 | **치명**<br>*(qa 미지적 · 기획자 추가)* | `Thanks. We run out of milk every week, so I buy some, too.`<br>`고마워요. 우유는 매주 떨어지니까 그것도 살게요.` | **영어 그대로**<br>`고마워요. 우유는 매주 떨어져서 저는 그것도 사요.` | 1번과 **같은 결함**이다 — 영어는 습관(`I buy`), 한국어만 즉석 결정(`살게요`). **한국어만** 고친다 |
| 3 | **9071** | 7 | 보통<br>*(qa 미지적 · 기획자 추가)* | `It is not urgent. The box is on the table by the window.`<br>`급한 건 아니에요. 상자는 창가 탁자에 뒀어요.` | **영어 그대로**<br>`급한 건 아니에요. 상자는 창가 탁자에 있어요.` | 영어는 **현재 상태**인데 한국어만 **과거**다. **과거 시제는 E2**(§2) — 한국어 뜻으로도 먼저 보여주지 않는다(§3-4 ⑤ 마지막 줄) |
| 4 | **9072** | 7 | **치명** | `Thanks. I open it after lunch.`<br>`고마워요. 점심 먹고 열어 볼게요.` | `Thanks. The package is fine there until lunch.`<br>`고마워요. 그 택배는 점심때까지 거기 있어도 괜찮아요.` | 즉석 결정 → **상태 확인**으로 옮겼다(§3-4 ⑤). 두 번째 언급이라 `the package` — **유닛 7의 핵심 문법(a → the)이 오히려 더 선명해진다** |
| 5 | **9074** | 7 | **치명** | `Then I read the letter first. I go home by bus at six.`<br>`그럼 편지부터 읽을게요. 저는 6시에 버스로 집에 가요.` | `Then I always read letters first. I go home by bus at six.`<br>`그럼 저는 원래 편지부터 읽어요. 6시에 버스로 집에 가거든요.` | 즉석 결정 → **습관**으로. 뒷문장(`by bus` · `go home` = 무관사 자리)은 **손대지 않는다** — 유닛 7 문법 2의 유일한 회화 증거다 |
| 6 | **9083** | 8 | **치명** | `Then I leave it on his desk. He sits by the window, right?`<br>`그럼 준 씨 자리에 둘게요. 준 씨가 창가에 앉죠?` | `Then it is Jun's, not yours. He sits by the window, right?`<br>`그럼 미나 씨 게 아니라 준 씨 거네요. 준 씨가 창가에 앉죠?` | 오퍼(돌려주겠다) → **소유 확정**으로. `Jun's`(소유격) · `yours`(소유대명사) · `He`(주격)가 한 줄에 들어와 **유닛 8 문법 1의 최고 증거**가 된다. 다음 줄 `He does.` 가 그대로 받는다 |
| 7 | **9105** | 10 | **필요**<br>*(장면 일치 · 기획자 추가)* | `Are you free on Friday?`<br>`금요일에 시간 되세요?` | `Our team meeting is on Friday at ten. Are you free in the morning?`<br>`팀 회의가 금요일 10시예요. 오전에 시간 되세요?` | **"회의가 이미 금요일 10시로 잡혀 있다"는 사실을 첫 줄에서 세워야** 장면 전체가 확인이 된다. 이게 없으면 9108만 고쳐도 대화는 여전히 **제안→합의**다. 덤으로 `on Friday`(요일 on) · `at ten`(시각 at) · `in the morning`(시간대 in)이 **유닛 10 문법 1 셋을 첫 줄 한 곳에서** 보여준다 |
| 8 | **9107** | 10 | **필요**<br>*(장면 일치 · 기획자 추가)* | `Then at ten? We meet in the small room on the third floor.`<br>`그럼 10시에요? 3층 작은 방에서 봐요.` | `Good. The meeting is in the small room on the third floor.`<br>`잘됐네요. 회의는 3층 작은 방에서 해요.` | `Then at ten?` 은 **제안**이다 — 시각이 1번 줄로 올라갔으니 이 줄은 **장소 확인**만 맡는다. `We meet ~`(습관인지 약속인지 불분명) → `The meeting is ~`(상태). `in the small room`(공간 in) + `on the third floor`(면 on)가 **유닛 10 문법 2의 교과서 예**가 된다 |
| 9 | **9108** | 10 | **치명** | `Ten is good. I put it in my schedule now.`<br>`10시 좋아요. 지금 일정에 넣을게요.` | `Ten is good. It is already in my schedule.`<br>`10시 좋아요. 제 일정에 이미 들어 있어요.` | 즉석 결정 → **이미 그렇게 되어 있다는 확인**으로(§3-4 ⑤). 1번 줄에서 벤이 일정을 짚어 주고 미나가 "이미 들어 있다"고 받는 것이라 **모순이 아니라 확인**이다. `in my schedule`도 **공간 in**이라 유닛 10 문법에 그대로 얹힌다 |

> **유닛 10 회화는 위 3줄을 한꺼번에 고쳐야 뜻이 통한다.** 고친 뒤 여섯 줄을 이어 읽으면 이렇게 된다 —
> ① 벤: 회의는 금요일 10시, 오전에 되세요? → ② 미나: 오전은 돼요, 점심 뒤엔 늘 바빠요 → ③ 벤: 잘됐네요, 3층 작은 방이에요 →
> ④ 미나: 10시 좋아요, 제 일정엔 이미 있어요 → ⑤ 벤: 자료는 늘 미리 보내요 → ⑥ 미나: 금요일 회의엔 절대 안 늦어요.
> **아무것도 새로 정하지 않는다**(§3-4 ③ 점검 한 줄). 그러면서 시간의 at/on/in(①) · 장소의 at/on/in(③) · 빈도부사 세 자리(②⑤⑥)가 전부 나온다.

**회화 제목(`dialog.title`) 2건** — 대사를 고치면 제목이 장면과 어긋난다.

| id | 유닛 | 현재 | 확정 새 값 | 왜 |
|---|---|---|---|---|
| **9010** | 10 | `금요일 몇 시에 될까요?` | **`금요일 10시 회의, 시간 되세요?`** | "몇 시에 될까요"는 **잡는** 말이다. 고친 장면(이미 금요일 10시로 잡힌 회의의 참석·장소 확인)과 맞추고, 첫 대사와 같은 말을 쓴다 |
| **9005** | 5 | `퇴근길 통화 — 어디서 볼까요?` | **`퇴근길 통화 — 어디서 만나요?`** | 대사는 2026-09-21에 확인형으로 고쳤는데(`We meet at six, right?` · `Where do we meet?`) **제목만 제안형으로 남았다.** 대사 9048의 한국어(`어디서 만나요?`)와 같은 말로 맞춘다 |

**경미 1건** — `expression_example` 9055(유닛 6 `some more`): 영어 `Do you want some more rice?` 는 **상대의 의향을 묻는** 말인데 한국어가 `밥 조금 더 드릴까요?`(내가 주겠다는 오퍼)다.
→ 한국어를 **`밥 조금 더 드실래요?`** 로. 영어는 그대로. (§8-3 16의 같은 규칙)

#### 3-5-2. `grammar_point.name_ko` — 부제 5건

> **왜 결함인가**: `name_ko`는 화면에서 **문법 제목 바로 밑**(`.step-subtitle`)과 **자료실 문법 목록**에 학습자 눈에 그대로 보인다.
> 유닛 1~5는 15개 전부 한국어 부제인데 **6~10에서만 영어 예시 4건**이 들어갔다 — **§3-3이 그 값을 그대로 지시했다.** 기준부터 틀렸던 것이다.
> `name` · `explanation` · 예문 · id · 매핑은 **손대지 않는다.** `name_ko` 한 칸씩만 바꾼다.

| id | 유닛 | 문법 `name` (그대로) | 현재 `name_ko` | **확정 새 `name_ko`** |
|---|---|---|---|---|
| **9028** | 6 | `a와 an은 철자가 아니라 소리로 고른다` | `an hour, a university` | **`첫소리로 고른다`** |
| **9032** | 7 | `관사를 아예 쓰지 않는 자리` | `have lunch, by bus, go home` | **`식사·교통수단·집에는 붙지 않는다`** |
| **9033** | 7 | `the가 통째로 굳은 자리` | `in the morning, on the phone` | **`따질 대상이 아니라 외울 대상`** |
| **9038** | 8 | `this / that / these / those` | `가까운 것과 먼 것` | **`거리로만 고르는 말이 아니다`** |
| **9043** | 9 | `위치는 문장 끝에 붙인다` | `There is a cafe on the first floor.` | **`무엇이 있는지를 먼저 말한다`** |

- **9038은 영어가 아니라 프레임이 문제다.** `가까운 것과 먼 것`은 **학교 버전**인데 바로 밑 설명이 *"실제로는 거리와 무관한 자리가 더 잦다"* 고 뒤집는다 —
  화면에서 **제일 큰 글씨 밑에 틀린 요약**이 남는다. 이 코스가 스스로를 반증하는 자리라 나머지 4건과 같은 무게로 고친다(§8-3 18).
- **qa 제안값과 3건이 다르다.** qa는 `관사를 붙이지 않는 자리` · `덩어리로 외우는 the` · `어디에 있는지는 뒤에 붙인다`를 제안했고 **셋 다 규칙은 만족한다.**
  다만 셋 다 **바로 위 `name`을 거의 그대로 되풀이한다**(`관사를 아예 쓰지 않는 자리` / `관사를 붙이지 않는 자리`는 같은 말이다).
  부제는 제목이 안 말한 것을 말해야 하므로(유닛 1~5의 확정값이 전부 그 모양이다 — `셀 수 있는 명사와 셀 수 없는 명사` / `a를 붙일 수 있나부터 본다`)
  **각 `explanation`이 실제로 내린 결론을 한 줄로 꺼내는 쪽**으로 확정했다. 9028만 qa 제안을 그대로 받았다.

#### 3-5-3. 유닛 11~15에서 같은 눈으로 봐야 할 것 (backend-dev 작업 중 영역 — **이번 커밋에 넣지 않는다**)

유닛 6~10을 훑으며 **같은 결함이 유닛 12에도 하나 보였다.** 지금 쓰고 있는 유닛이라 **여기서 고치지 않고 유닛 11~15 검수 때 함께** 처리한다.

| id | 유닛 | 현재 | 권고 | 왜 |
|---|---|---|---|---|
| `expression_example` **9114** | 12 (`go ahead`) | `Go ahead, I wait here.`<br>`먼저 하세요, 저는 여기서 기다릴게요.` | `Go ahead, I can wait here.`<br>`먼저 하세요, 저는 여기서 기다려도 돼요.` | 영어는 단순현재(습관), 한국어만 의향이다 — §3-5-1과 같은 결함. **다만 유닛 12에는 `can`이 있어** 문장으로 풀 수 있다. 장면을 바꿀 필요가 없다 |

> **유닛 11~15는 유닛 6~10과 검수 기준이 다르다.** 12부터는 제안·부탁·의향을 담을 문법이 생기므로,
> 한국어에 `-ㄹ게요`가 보이면 **장면을 의심하기 전에 "그 유닛의 문법으로 안 쓴 이유"를 먼저 묻는다.**
> `Let me check.` 를 써야 할 자리를 `I check.` 로 때웠다면 그건 **장면 문제가 아니라 문장 문제**다(AC-31).

---

## 4. 중복 금지 장부 — 이미 쓴 표기 (코스 1 안에서 다시 쓸 수 없다)

**코스 내부 중복은 테스트가 잡는다**(`EnglishContentRuleIntegrationTest` #4). 유닛 3~15를 쓰기 전에 이 목록을 옆에 두고 작업한다.

**표현 12개 (유닛 1·2)**
`get up` · `be good at` · `on my way` · `a couple of` · `take a break` · `right away` · `hang out` · `be into` · `how come` · `kind of` · `come up with` · `no big deal`

**어휘 30개 (유닛 1·2)**
morning · meeting · team · desk · floor · work · live · start · usually · early · busy · nervous · near · between · because
weekend · movie · hobby · plan · enjoy · spend · practice · often · never · together · instead · tired · free · during · but

**표현 21개 (유닛 3·4·5 — 2026-09-21 시드 실측)**

- 유닛 3: `be from` · `work for` · `in charge of` · `not really` · `for sure` · `by the way` · `look like`
- 유닛 4: `how about` · `take a look` · `be about to` · `on foot` · `stop by` · `eat out` · `in line`
- 유닛 5: `pick up` · `get on` · `get off` · `on time` · `head home` · `run into` · `give a ride`

**어휘 48개 (유닛 3·4·5 — 2026-09-21 시드 실측)**

- 유닛 3: name · office · manager · colleague · guest · introduce · meet · ask · friendly · sure · same · right · actually · also · still · with
- 유닛 4: lunch · restaurant · menu · price · minute · coffee · order · walk · wait · take · far · cheap · expensive · close · already · across
- 유닛 5: station · bus · subway · street · corner · exit · arrive · leave · drive · cross · late · crowded · outside · away · along · or

> **유닛 6 이후 분은 여기에 없다.** 이 장부는 **시드를 따라가는 사본**이지 원본이 아니다 —
> 새 유닛을 쓰기 전에 `data-course-en-content.sql`의 `expression.text` · `vocabulary.word`를 훑어 **먼저 덧붙이고** 시작한다.
> 문서와 시드가 어긋나면 **시드가 진실**이고, 최종 판정은 `EnglishContentRuleIntegrationTest` 규칙 4가 한다.

- **문법·회화·예문 본문에는 얼마든지 다시 나와도 된다.** 금지되는 것은 **표현·어휘의 표제어**를 다시 올리는 것뿐이다.
- 유닛 3~15를 쓰면서 **이 장부에 새로 쓴 표기를 계속 덧붙인다.** 유닛 13쯤 가면 "이미 썼나"가 사람 기억으로 감당이 안 된다.
- **코스 간 중복은 허용**이다(§11-5) — E2에서 `get up`이 다른 뉘앙스로 다시 나오는 것은 정당하다.

---

## 5. 사용자 흐름 (유닛 3~5가 열린 뒤)

1. **`/en/courses`(영어 코스 목록)** 에서 「다시 세우기」 카드를 누른다 → **코스 상세**로 이동.
2. **코스 상세**에서 유닛 목록이 **1~5** 로 보이고, 맨 위에 안내 한 줄(`앞 유닛부터 순서대로 채우는 중이에요 …`)이 붙어 있다 → **유닛 행**을 누른다.
3. **유닛 학습**에서 **문법 → 회화 → 표현 → 어휘 → 정리** 순으로 [다음 ›]을 누른다 → 정리 스텝에서 **완료**로 켜지고 [다음 유닛]이 보인다.
4. 유닛 4의 [다음 유닛] → **유닛 5**. 유닛 5의 **정리 스텝**에서 **1~5 복습 블록**을 본다 → 코스 상세로 돌아가면 5개 행 모두 완료 표시.
5. 유닛 5의 정리에는 **다음 유닛이 없다** → 여기가 지금 열린 데까지의 끝이라는 것이 화면으로 보인다.
6. (부록) **`/en/library/expressions`** 에서 새로 들어온 표현이 검색되고, 각 항목이 **"코스 1 · 유닛 N"** 을 함께 보여준다 → 거기서 코스로 돌아갈 수 있다.

## 5-1. 화면 목록

**새 화면은 0개다.** 이 기획은 화면을 만들지 않고 **데이터만 채운다**(플레이리스트 모델 — 01 §4).

| 화면 | 역할 | 새/수정 |
|---|---|---|
| 영어 코스 목록 `/en/courses` | 카드의 유닛 수가 2 → 5로 바뀐다 | **기존, 코드 수정 없음**(데이터에서 센다) |
| 영어 코스 상세 `/en/courses/101` | 유닛 행 5개 · 안내 문구 교체 | **기존, 문구는 `course.notice` 데이터** |
| 영어 유닛 학습 `/en/courses/101/units/3~5` | 새 유닛 3개 | **기존 화면, 데이터만 추가** |
| 영어 자료실 3탭 | 표현·문법·어휘가 늘어난다 | **기존, 코드 수정 없음** |

> **화면 코드를 고쳐야 할 일이 생기면 그 자체가 신호다** — 이 기획의 전제(뼈대는 완성, 콘텐츠만 채운다)가 깨진 것이므로 senior-dev에게 돌린다.
> **그 신호가 실제로 울렸다** → §5-5.

---

## 5-5. 열린 데까지의 끝 — 정리 스텝의 세 문구 (2026-09-21 확정)

**무엇이 문제였나.** 유닛 5의 정리 스텝에 **🎉 "다시 세우기 코스를 끝까지 봤어요!"** 가 떴다.
같은 코스의 상세 화면은 정반대로 *"앞 유닛부터 순서대로 채우는 중이에요"* 라고 말한다 — **한 학습자가 두 화면에서 모순된 안내**를 받는다.
유닛이 다음 유닛을 못 찾은 것("다음이 없다")을 화면이 코스가 끝난 것("다 봤다")으로 읽은 것이고, **예외 E-4(거짓 안내를 만들지 않는다) 위반**이다. 유닛 10에서도 그대로 재발한다.

**계약**(senior-dev · `../설계/04 §2-3`): 응답에 **그 코스가 최종적으로 갖게 될 유닛 수**가 함께 내려온다(E1 = 15, 값이 없으면 *지금 있는 게 전부*).
그래서 화면이 **"코스를 완주했다"** 와 **"지금 열린 데까지 왔다"** 를 구분할 수 있다. **영어 전용 분기가 아니다** — 일본어 코스를 부분 공개해도 같게 동작한다.

### 세 상태

| # | 언제 뜨나 | 사용자가 보는 문구 | 진행 버튼이 가는 곳 |
|---|---|---|---|
| **1. 완주** | 계획한 유닛을 **다 채운** 코스의 마지막 유닛 정리 (계획값이 없는 코스 = 일본어 전 코스도 여기) | **`🎉 {코스명} 코스를 끝까지 봤어요!`** + (다음 코스가 열려 있으면) `다음 코스 {코스명}(으)로 바로 이어갈 수 있어요.` / (다음 코스가 아예 없으면) `준비된 모든 코스를 완주했어요.` — **지금 그대로 유지** | 다음 코스 상세 또는 코스 목록 (지금 그대로) |
| **2. 열린 데까지 도달** | 그 코스에 **아직 안 열린 유닛이 남아 있는데** 마지막 열린 유닛의 정리에 섰다 (E1 유닛 5·10) | **`여기까지가 지금 열려 있는 마지막 유닛이에요. 다음 유닛은 앞 유닛부터 순서대로 채우는 중이에요 — 그동안은 자료실에서 지금까지 배운 것을 다시 볼 수 있어요.`** | **그 코스의 유닛 목록**(코스 상세). 버튼 문구는 **`유닛 목록으로 ›`** |
| **3. 완주했는데 다음 코스가 준비중** | 상태 1의 한 갈래 — 코스는 정말 끝났고 다음 코스가 아직 안 열렸다 (E1이 15유닛을 채운 뒤, E2가 열리기 전) | `🎉 {코스명} 코스를 끝까지 봤어요!` `다음 코스 {코스명}은/는 지금 준비하고 있어요.` **+ 한 문장 추가: `그동안은 자료실에서 지금까지 배운 것을 다시 볼 수 있어요.`** | 코스 목록 (지금 그대로) |

**세 상태는 동시에 뜨지 않는다.** 2가 뜨는 자리에서는 1·3의 문장이 **하나도** 나오지 않는다.

### 2와 3은 무엇이 다른가 (같은 "끝"처럼 보이지만 사실이 다르다)

| | 2. 열린 데까지 도달 | 3. 다음 코스 준비중 |
|---|---|---|
| **사실** | 이 코스는 **안 끝났다.** 뒤에 유닛이 더 온다 | 이 코스는 **끝났다.** 다음 *코스*가 아직 없다 |
| **기다리는 것** | 같은 코스의 다음 유닛 | 다음 코스 |
| **축하하나** | **아니다.** 🎉도 축하 색도 쓰지 않는다 — 안 한 것을 했다고 말하게 된다 | **한다.** 완주는 사실이다 |
| **어디로 보내나** | **그 코스 안**(유닛 목록) — 남은 열린 유닛을 다시 볼 수 있는 **유일하게 참인 행선지** | 코스 목록 |

### 문구를 이렇게 고른 이유

1. **코스 상세와 같은 말을 한다.** 상세의 안내(`앞 유닛부터 순서대로 채우는 중이에요 …`)와 **같은 동사·같은 시제**를 쓴다 — 이번 수정의 요점은 두 화면이 **같은 사실을 같은 목소리로** 말하는 것이다. 문구를 새로 지어내면 모순이 형태만 바꿔 살아남는다.
2. **숫자를 적지 않는다**(`../설계/06 §11-12 ③` R1). "5/15", "10개 남았어요"를 쓰면 화면이 **이미 데이터에서 세고 있는 값**과 갈리는 자리가 하나 더 생긴다. 유닛이 늘어날 때마다 고쳐야 하는 문장은 결국 안 고쳐진다.
3. **학습자를 탓하지 않는다.** "여기까지가 **지금 열려 있는** 마지막 유닛"은 **제품의 상태**를 말하지 학습자의 진도를 말하지 않는다. *"더 진행할 수 없어요"* 처럼 쓰면 학습자가 자기가 뭘 잘못했는지 찾는다.
4. **실망 대신 지금 할 수 있는 것을 말한다**(`../설계/05 §12` — 결핍을 사과하지 않는다). "아직 없어요" · "준비가 안 됐어요"를 쓰지 않고 **자료실**이라는 실재하는 행선지를 준다. 자료실은 방금 배운 표현·어휘가 그대로 검색되는 곳이라(§5 흐름 6) 빈 약속이 아니다.
5. **"배운 것"이라고만 쓴 이유**: 같은 문장이 일본어 코스에도 쓰인다. "표현과 어휘"라고 쓰면 일본어(한자·문법·어휘)에서 거짓이 된다.

### 지켜야 할 선

- 상태 2에 **🎉·축하 색(`tone="ok"`)을 쓰지 않는다.** 안내 톤이다. (구체 표현은 designer 소관)
- 상태 2에서 **다음 코스로 내보내지 않는다.** 코스가 끝나지 않았는데 다음 코스로 보내는 것은 버튼으로 하는 거짓말이다.
- 상태 3의 앞 두 문장은 **글자 그대로 유지한다** — 기존 테스트 두 곳(`EnUnitStudy.test.jsx` · `UnitStudyPage.test.jsx`)이 이 문장을 고정하고 있다. **뒤에 한 문장을 덧붙이는 방식으로만** 반영하고, 그래도 테스트가 깨지면 손대지 말고 **senior-dev에게 돌린다**(TDD 규칙 2).
- **E1이 15유닛을 채우면** 상태 2는 이 코스에서 영영 뜨지 않고, 코스 상세의 안내 문구도 `NULL`로 지운다(`../설계/06 §11-12 ③` R4). **두 자리를 같은 커밋에서** 손본다.

---

## 6. 예외 상황 — 사용자가 무엇을 보나

| # | 상황 | 사용자에게 보이는 것 / 어디로 |
|---|---|---|
| E-1 | 아직 안 만든 유닛 주소(`/en/courses/101/units/6`)로 직접 들어간다 | **없는 페이지 안내**(일본어 유닛과 같은 처리). **일본어 코스로 튕기지 않는다.** ⚠️ 현재 동작 확인 필요 — qa 항목 |
| E-2 | 준비중 코스(102~105) 주소로 직접 들어간다 | 지금 그대로 — **열린 영어 코스로 가는 길**을 보여준다(01 §10-2). 이 기획으로 바뀌지 않는다 |
| E-3 | 유닛 5는 있는데 1~4 중 하나가 빠진 채 배포됐다 | **복습 블록이 빈다.** 학습자에게는 "정리인데 되짚을 게 없는 화면"이 된다 → **5의 배수 경계로만 공개**하는 이유. 배포 전 유닛 수를 세어 막는다(§8 AC-8) |
| E-4 | 안내 문구가 "맛보기 2개"인데 유닛이 5개다 | **거짓 안내.** 유닛 증설과 **같은 커밋**에서 문구를 바꾼다(§1-3). 두 상태가 동시에 존재하는 배포를 만들지 않는다 |
| E-5 | 표현·어휘가 유닛 1·2와 겹쳤다 | 학습자는 **같은 단어를 두 번 배우고** 자료실에서 같은 항목을 두 줄로 본다 → 테스트가 빌드에서 잡는다(§11-11 #4) |
| E-6 | 발음 표기 한쪽만 채웠다 | IPA만 있으면 못 읽는 학습자가 있고 한글만 있으면 잘못된 발음이 굳는다 → 테스트가 빌드에서 잡는다(§11-11 #2) |
| E-7 | 회화에 그 유닛 문법이 안 나온다 | 학습자가 **방금 배운 것이 어디 쓰이는지** 못 본다. 자동으로 안 잡힌다 — **사람이 검수**한다(§8 AC-5) |
| E-8 | 통신이 끊긴 상태로 유닛에 들어간다 | 기존 오류 처리 그대로. 이 기획으로 바뀌는 것이 없다 |
| **E-9** | **마지막 열린 유닛(5·10)의 정리에서 화면이 "코스를 끝까지 봤다"고 말한다** | **E-4와 같은 종류의 거짓 안내**(코스 상세는 "채우는 중"이라고 말한다 — 한 학습자가 두 화면에서 모순된 안내를 받는다) → **§5-5 상태 2 문구**를 보여주고 **그 코스의 유닛 목록**으로 보낸다. 다음 코스로 내보내지 않는다 |
| **E-10** | **아직 안 열린 유닛에서 이어서 학습하기·복습 블록이 그 유닛을 가리킨다** | 지금은 일어나지 않는다(열린 유닛만 데이터에 있다). **부분 공개가 "유닛을 미리 만들어 두고 감추는" 방식으로 바뀌면 이 예외가 살아난다** — 그때는 이 기획을 다시 연다 |

---

## 7. 인수 조건 (qa가 그대로 체크리스트로 쓴다)

**유닛 3~5 공개 기준**

- [ ] **AC-1** `/en/courses`의 「다시 세우기」 카드에 유닛 수가 **5**로 보인다.
- [ ] **AC-2** 코스 상세의 유닛 목록이 **1·2·3·4·5** 순서로 보이고, 각 행을 누르면 그 유닛 학습 화면이 열린다.
- [ ] **AC-3** 유닛 3·4·5의 스텝 바가 **문법 → 회화 → 표현 → 어휘 → 정리**이고, **확인 문제 스텝이 없다.** 화면 어디에도 음성(재생) 버튼이 없다.
- [ ] **AC-4** 유닛 3·4·5 각각 **문법 2~3개 · 회화 1편 · 표현 6~10개 · 어휘 15~20개**가 실제로 보인다.
- [ ] **AC-5** 유닛 3·4·5의 회화에 **그 유닛에서 배운 문법이 실제로 쓰인 문장**이 각각 하나 이상 있다. (사람 검수)
- [ ] **AC-6** 회화가 **화자 2명 이상 · 4줄 이상 12줄 이하**다.
- [ ] **AC-7** 유닛 3·4·5의 표현·어휘 어디에도 **§4 장부의 표기가 다시 나오지 않는다.**
- [ ] **AC-8** 유닛 **5의 정리 스텝에 1~5 복습 블록**이 보이고, 그 안의 항목이 실제로 1~5에서 배운 것이다.
- [ ] **AC-9** 유닛 3·4·5의 **표현·어휘 전량**에 IPA와 한글 근사가 **둘 다** 보인다. 어디에도 가나(kana) 줄이 없다.
- [ ] **AC-10** 문법 설명마다 **"학교에서 배운 방식"과 "실제 쓰임"의 대비**가 읽힌다. (사람 검수 — 이 코스의 정체성)
- [ ] **AC-11** 코스 상세 안내 문구에 **숫자가 없고**, "맛보기 2개"라는 말이 화면·데이터 어디에도 남아 있지 않다.
- [ ] **AC-12** `/en/library/expressions`·`/vocabulary`에서 새 항목이 검색되고, 각 항목이 **"코스 1 · 유닛 N"** 을 보여준다.
- [ ] **AC-13** 없는 유닛 번호(`/units/6`)로 직접 들어가면 **없는 페이지 안내**가 나오고, 일본어 코스로 가지 않는다.
- [ ] **AC-14** 유닛 3·4·5를 정리까지 보면 **완료 표시가 켜지고**, 홈의 [이어서 학습하기]가 그 유닛을 가리킨다(비로그인 포함).

**문법 용어·톤 (2026-09-21 검수로 추가)**

- [ ] **AC-18** 유닛 4의 둘째 문법 이름이 **`how + 한 마디 더 (how much / how long / how often)`** 이고, **"형용사"라는 말이 그 이름에 없다.** 유닛 5 정리의 **복습 칩에도 없다.**
- [ ] **AC-19** 유닛 5 첫 문법(어순)의 설명과 예문에 **대명사 쌍(I·me / she·her)이 자리 비교용으로 쓰이지 않는다.** 비교 쌍의 **두 문장이 같은 단어로만 이루어져 있다.**
- [ ] **AC-20** **유닛 1·2를 포함해 문법 13개 전부**가 AC-10을 만족한다 — 특히 9002·9003·9004의 설명이 **"학교에서는 …"으로 시작**한다. (사람 검수)

**부분 공개 코스의 끝 안내 (§5-5)**

- [ ] **AC-21** 유닛 5의 정리 스텝에 **완주를 주장하는 말이 하나도 없다** — 🎉 · "끝까지 봤어요" · "완주했어요" · "바로 이어갈 수 있어요" · "지금 준비하고 있어요" 어느 것도 보이지 않는다.
- [ ] **AC-22** 대신 **§5-5 상태 2 문구**가 보이고, 그 안에 **숫자가 없다.**
- [ ] **AC-23** 그 화면의 진행 버튼이 **[유닛 목록으로 ›]** 이고 누르면 **코스 상세**로 간다. [다음 코스 … 시작하기]가 없다.
- [ ] **AC-24** 유닛 3·4(마지막 열린 유닛이 아니다)의 정리에는 그 안내가 **없고** [다음 유닛 ›]이 그대로다.
- [ ] **AC-25** **일본어 코스는 하나도 안 바뀐다** — 마지막 유닛(예: N5 유닛 20)의 정리에 지금 그대로 완주 축하가 뜬다.
- [ ] **AC-26** **코스 상세 → 유닛 5 정리**를 이어서 보면 두 화면이 **같은 사실**을 말한다(한쪽은 "채우는 중", 다른 쪽은 "다 봤다"가 아니다). (사람 검수 — 이번 결함의 본체)

**회화가 영어로 말이 되는가 · 부제가 학습자에게 읽히는가 (2026-09-22 검수로 추가)**

- [ ] **AC-28** 유닛 **1~11**의 회화 대사 한국어 뜻에 **`-ㄹ게요` · `-겠어요` · `~할까요` · `~드릴까요`** 로 끝나는 줄이 **하나도 없다.** (영어가 단순현재인데 한국어만 의향·오퍼인 짝이 없다)
- [ ] **AC-29** 유닛 **1~11**의 회화 대사 한국어 뜻에 **과거 시제가 없다**(`뒀어요` · `봤어요` · `했어요`). E1은 현재 시제 코스이고 과거는 E2다.
- [ ] **AC-30** 유닛 **1~11**의 회화가 **"두 사람이 무엇을 새로 정하는" 장면이 아니다** — 제안 → 반대 제안 → 합의 구조가 없다. (사람 검수 · §3-4 ③)
- [ ] **AC-31** 반대로 **유닛 12~15**의 회화에는 제안·부탁이 **그 유닛의 문법으로**(`can` · `Can you ~?` · `Let's` · `Let me` · `Why don't we ~?`) 표현돼 있고, **단순현재로 제안을 대신한 줄이 없다.**
- [ ] **AC-32** 문법 **부제(`name_ko`) 전량이 한국어**다 — 문법 스텝 화면(제목 바로 밑)과 **자료실 문법 목록** 두 곳 다에서 영어 예문·예구가 보이지 않는다.
- [ ] **AC-33** 부제가 **바로 밑 설명에 반박당하지 않는다** — 설명이 "실제로는 그렇지 않다"고 뒤집는 프레임(학교 버전)을 부제가 쓰고 있지 않다. (사람 검수)
- [ ] **AC-34** 부제가 **문법 제목을 되풀이하지 않는다** — 제목과 부제를 이어 읽었을 때 정보가 하나 더 늘어난다. (사람 검수)

**E1 완성(15유닛) 기준** — 위 전부 + 아래

- [ ] **AC-15** 유닛 목록이 1~15이고 **10·15의 정리에도 복습 블록**이 붙는다.
- [ ] **AC-16** 코스 상세에 **안내 문구가 더 이상 없다**(`notice`가 비었다).
- [ ] **AC-17** 코스 1 안에서 **표현·어휘 표기가 단 하나도 중복되지 않는다**(빌드 테스트 + 눈 검수).
- [ ] **AC-27** 유닛 15의 정리에 **완주 축하(상태 1)** 가 뜨고 **§5-5 상태 2 문구는 사라진다.** E2가 아직 준비중이면 **상태 3 문구**(자료실 한 문장 포함)가 보인다.

---

## 8. backend-dev 인계

### 8-1. 이번에 쓸 것

> **진행 상태 (2026-09-21 기준)** — 유닛 3·4·5는 **반영됐다**(커밋 `693cca5`). 그 뒤 **콘텐츠 검수(qa)에서 나온 수정 3건**이 아래에 있다. **이 세 건은 전부 이 기획서가 틀려서 생긴 것**이므로 콘텐츠 작업자의 결함으로 세지 않는다.
>
> | 무엇 | 어디 | 고칠 곳 |
> |---|---|---|
> | 유닛 4 문법 `name` → `how + 한 마디 더 (how much / how long / how often)` | §3-3 유닛 4 ⚠️ | 시드 `grammar_point` 9017의 `name` 한 줄 |
> | 유닛 5 어순 예시에서 대명사 쌍 제거 → `The manager knows Mina.` / `Mina knows the manager.` | §3-3 유닛 5 ⚠️ | 시드 9021 `explanation` + 예문 9061·9062 |
> | 유닛 1·2 문법 설명 소급 보강(9002·9003·9004) | §3-3 머리 | 시드 `explanation` 3개 (id·`name`·예문은 손대지 않는다) |
>
> **셋을 같은 커밋에서 낸다.** 유닛 6~10 작업과 섞지 않는 편이 되돌리기 쉽다.
>
> **진행 상태 (2026-09-22 갱신)** — 유닛 6~10도 반영됐고(커밋 `9b5b198`), 그 **콘텐츠 검수(qa)에서 나온 수정이 §3-5**에 있다.
> **이것도 전부 이 기획서가 틀려서 생긴 것**이다 — §3-1·§3-3의 장면 지시가 §3-4 ③과 어긋난 채 남아 있었다. **콘텐츠 작업자의 결함으로 세지 않는다.**
>
> | 무엇 | 몇 건 | 고칠 곳 |
> |---|---|---|
> | **회화 대사** — 영어 단순현재에 한국어 의향 어미(`-ㄹ게요`)를 붙인 줄 | **치명 5 · 보통 1 · 장면 일치 2 = 9줄** | §3-5-1 (`dialog_line` 9061·9062·9071·9072·9074·9083·9105·9107·9108) |
> | **회화 제목** — 장면이 "잡는다"로 읽히는 제목 | 2 | §3-5-1 (`dialog` 9010·9005) |
> | **문법 부제(`name_ko`)** — 영어 예시 4 + 설명이 반박하는 프레임 1 | 5 | §3-5-2 (`grammar_point` 9028·9032·9033·9038·9043) |
> | 표현 예문 한국어 뜻 — 의향/오퍼 어긋남 | 1(경미) | §3-5-1 끝 (`expression_example` 9055) |
>
> **문자열만 바뀐다 — id·매핑·개수는 하나도 안 바뀐다.** 진도·복습 칩·자료실·수량 테스트에 파급이 없다.
> **유닛 11~15 작업과 같은 커밋에 섞지 않는다.**

**유닛 3 · 4 · 5 — 세 개.** 한 번에 15개를 쓰지 않는다.

| 항목 | 유닛 3개 분량 |
|---|---|
| 문법 | 9개 (유닛당 3) |
| 문법 예문 | 27개 (문법당 3 — 실측 관행) |
| 회화 | 3편 / 대사 **12~18줄** (유닛당 4줄 이상) |
| 표현 + 예문 | **18~30 + 18~30** |
| 어휘 | **45~60** |
| **발음 표기** | **표현 + 어휘 = 유닛당 21~30건 × 2표기(IPA·한글) = 세 유닛에 126~180개 값** |

> **발음 표기가 이 작업의 실제 부피다.** 유닛 하나당 21~30건에 각각 IPA와 한글 근사를 **둘 다** 채워야 하고,
> 한쪽만 채운 데이터는 미완성으로 본다(§11-3). 15유닛을 다 채우면 **표현·어휘 315~450건 = 발음 값 630~900개**가 된다.
> 한 번에 몰아서 쓸 분량이 아니라서 **5유닛 단위로 끊는다.**

### 8-2. ID 대역 제안 (§11-10: 9000~9999 · 현재 9001~9030 사용)

**유닛 1·2는 건드리지 않는다.** 유닛 3부터 아래 규칙으로 배정한다 — **유닛마다 블록을 통째로 잡아** 나중에 유닛 안에서 항목이 늘어도 뒤 유닛과 부딪히지 않게 한다. 빈 번호가 남는 것은 문제가 아니다.

| 테이블 | 유닛당 블록 | 유닛 n(n≥3) 시작 | 유닛 3 | 유닛 15 끝 |
|---|---:|---|---|---:|
| `grammar_point` | 5 | `9011 + (n-3)*5` | 9011~9015 | 9075 |
| `grammar_example` | 15 | `9031 + (n-3)*15` | 9031~9045 | 9225 |
| `dialog` | 1 | `9000 + n` | 9003 | 9015 |
| `dialog_line` | 12 | `9021 + (n-3)*12` | 9021~9032 | 9176 |
| `expression` | 10 | `9021 + (n-3)*10` | 9021~9030 | 9150 |
| `expression_example` | — | **표현 id와 같은 수**(표현당 예문 1개 원칙) | 9021~9030 | 9150 |
| `vocabulary` | 20 | `9031 + (n-3)*20` | 9031~9050 | 9290 |
| `course_unit` | 1 | `9000 + n` | 9003 | 9015 |
| `unit_grammar` · `unit_expression` · `unit_vocabulary` | — | **매핑 행 id = 참조하는 콘텐츠 id와 같은 수** (현행 시드의 관행 그대로) | — | — |

- 표현당 예문을 2개 이상 쓰고 싶으면 **먼저 senior-dev에게 대역을 물어본다.** 임의로 옆 번호를 빌려 쓰지 않는다.
- **9000 대역은 E1 + E2까지만 감당한다.** 어휘 기준 E1이 ~9290, E2 20유닛이 ~9690을 쓰면 **E3에서 9999를 넘는다.**
  → **E3 착수 시점에 새 대역 배정이 필요하다**(senior-dev·`03 §7-2` 소관). E1 작업 중에는 문제되지 않는다.

### 8-3. 반드시 지킬 것 (어기면 빌드가 깨지거나 qa가 되돌린다)

1. **`kana`는 전량 `NULL`.** 하나라도 채우면 영어 데이터가 거부된다(§11-3).
2. **발음은 `ipa`(슬래시로 감쌈) + `ko_approx` 둘 다.** 장모음은 `-`, 단어 경계는 띄어쓰기(§11-3).
3. **품사는 영어 7종만.** `I_ADJECTIVE`·`NA_ADJECTIVE`·`EXPRESSION`을 쓰면 실패한다(§11-6).
4. **표현·어휘는 코스 1 안에서 중복 금지** — §4 장부를 옆에 두고 쓴다.
5. **한자 관련 테이블에 행을 만들지 않는다**(§11-4).
6. **확인 문제·음성을 전제한 콘텐츠를 만들지 않는다**(§11-9). 오답 후보를 미리 만들어 두는 작업은 하지 않는다.
7. **문법 `name`은 영어 명칭 + 한국어 혼용**(`be동사 의문문 (Am / Is / Are ~?)`), **`name_ko`는 학습자가 읽는 한국어 부제**. 둘 다 필수(§11-8).
8. **`grammar_example.jp` · `dialog_line.jp`에는 영어 문장**이 들어간다(이름이 `jp`인 것은 기록된 빚 — §11-3).
9. **표현 `text`는 덩어리 그 자체**만 적는다. 덩어리에 원래 든 관사·소유격은 남기고(`a couple of` `on my way`), 주어·목적어를 채우는 대명사는 붙이지 않는다.
10. **관사·한정사(a / the / some / any)를 어휘 표제어로 올리지 않는 것을 권장한다** — 품사 7종에 한정사가 없어 분류가 억지가 된다. 문법 스텝에서 가르치고 어휘는 명사·동사·형용사·부사·전치사·접속사로 채운다.
11. **`notice` 교체는 유닛 증설과 같은 커밋에서** 한다(§1-3 · 예외 E-4).
12. 시드 실행 순서 고정: `data-course-en-content.sql` → `data-course-en-units.sql`(§11-10).
13. **문법 `name`에 품사 이름을 쓰려면 정말 그 품사인지 확인한다.** 확신이 없으면 품사 용어를 쓰지 말고 **역할로 적는다**(`how + 한 마디 더`).
    이 코스는 **잘못 배운 용어를 바로잡는다**고 내걸었고, `name`은 **복습 칩에 다시 뜬다**(2026-09-21 치명 1건이 정확히 이 모양이었다 — §3-3 유닛 4).
14. **어순·자리를 보이는 예시에 대명사 쌍을 쓰지 않는다**(`I love you` / `You love me` · `I know her` / `She knows me`).
    대명사는 자리에 따라 **모양이 바뀌고** 그건 **유닛 8**의 내용이라, 유닛 5의 주장("같은 단어인데 자리만 다르다")을 스스로 뒤집는다. **3인칭 이름·명사 두 개**를 쓴다(§3-3 유닛 5).
15. **경계 밖 문형을 회화가 요구하게 두지 않는다.** 장면을 정하기 전에 **§3-4**를 본다 — 제안·의향은 유닛 12(can) 전까지 `how about` + 명사 하나뿐이다.
    **유닛 1~11의 회화는 "이미 정해진 일의 세부를 확인·조율"** 하는 장면만 쓰고, **제안 장면은 유닛 12부터 정상**이다(경계표는 §3-4 ③).
16. **한국어를 먼저 쓰고 영어를 맞추지 않는다.** 영어를 쓰고, **그 영어가 실제로 뜻하는 바**를 한국어(`meaning_ko`)로 적는다.
    한국어에 **`-ㄹ게요` · `-겠어요` · `~할까요` · `~드릴까요`** 같은 **의향·제안·오퍼 어미**가 들어갔다면,
    **그 뜻을 담을 영어 도구가 그 유닛에 있는지 먼저 확인한다** — `will`·진행형 = **E2** / `should`·`would` = **E4** / `can` = **유닛 12** / `want to`·`have to` = **유닛 13** / `Let's`·`Let me`·`Why don't we` = **유닛 14**.
    **없으면 문장이 아니라 장면을 바꾼다.**
    영어 **단순현재는 습관·사실**이지 즉석 결정·오퍼가 아니다 — `I carry the water`는 "제가 들게요"가 아니라 **"저는 (평소) 물을 나른다"** 다.
    그 짝을 시드에 남기면 학습자는 **`I carry the water = 제가 들게요` 라는 틀린 대응을 외운다.** 틀린 영어를 가르치는 것이라 결함 중 가장 무겁다(2026-09-22 치명 5건 — §3-5-1).
    **과거·미래도 같다** — `The box is on the table.` 에 "상자는 탁자에 **뒀어요**"를 붙이지 않는다. 한국어 뜻으로도 **E1이 안 가르친 시제를 먼저 보여주지 않는다.**
    → 궁지에 몰렸을 때 꺼내 쓸 **우회표는 §3-4 ⑤**에 있다(오퍼 → 상태·역할 / 즉석 결정 → 이미 그렇다는 확인 / 미래 약속 → 현재 상태 서술 / 과거 → 현재 상태 서술).
17. **`name_ko`에 영어를 넣지 않는다.** `name_ko`는 **학습자가 읽는 한국어 부제**이고, **문법 제목 바로 밑**(`.step-subtitle`)과 **자료실 문법 목록**에 그대로 보인다.
    `an hour, a university` · `have lunch, by bus, go home` · `There is a cafe on the first floor.` 처럼 **영어 예시를 부제 자리에 넣지 않는다** —
    학습자는 제목 밑에서 **설명이 아니라 문제를 하나 더** 본다. 영어 예시는 `explanation` 본문과 `grammar_example`이 이미 들고 있다(2026-09-22 검수 4건 — §3-5-2).
18. **부제는 제목을 되풀이하지 않고, 바로 밑 설명이 반박하는 프레임을 쓰지 않는다.**
    `name`이 이미 말한 것을 `name_ko`가 다시 말하면(`관사를 아예 쓰지 않는 자리` / `관사를 붙이지 않는 자리`) 부제 한 줄이 통째로 낭비된다 — **부제는 그 설명이 내린 결론을 꺼내는 자리**다.
    그리고 설명이 *"학교에서는 X로 배웠지만 실제로는 X가 아니다"* 라고 말하는데 **부제가 X(학교 버전)를 쓰고 있으면**, 화면에서 **제일 큰 글씨 밑에 틀린 요약**이 남는다.
    실제 사고: `this / that / these / those` 의 부제 `가까운 것과 먼 것` — 바로 밑 설명이 *"실제로는 거리와 무관하게 굳어 쓰는 자리가 더 잦다"* 고 뒤집는다.
    **부제는 그 유닛이 도달시키려는 쪽(실제 쓰임)을 적는다** — 확정값 `거리로만 고르는 말이 아니다`.

### 8-4. 같이 고쳐야 하는 문서 (유닛이 늘면 아래가 즉시 거짓이 된다)

| 문서 | 무엇이 낡나 | 담당 |
|---|---|---|
| `../설계/01 §10-2` | "AVAILABLE — 맛보기 · 2유닛" 표 · "맛보기 2유닛 안의 콘텐츠 실측" 문단 · `"지금은 맛보기 2개 유닛만 열려 있어요"` 인용 | product-planner |
| `../설계/01 §10-6` | "코스별 유닛 수 — 영어는 미정" 줄 → **확정값으로**(원본은 06 §11-12) | product-planner |
| `../설계/06 §11-2` | 코스 101 상태 "AVAILABLE (맛보기 2유닛)" | product-planner |
| `../설계/06 §11-4` | 실측 열(유닛당 2 / 계 4 등) | backend-dev(실측값) |
| `../진행사항/현재_기능현황.md` · `README.md` | "표현 12건" · "맛보기 2유닛" 수치 | backend-dev(실측값) |

> **수치는 코드·API 응답에서 센 것만 적는다**(진행사항 README §5-4).

---

## 9. 다음 코스 착수 순서 권고

1. **E1 15유닛을 먼저 끝낸다.** 코스는 순서가 있는 한 줄이고(01 §2), 앞 코스가 비어 있는데 다음 코스를 열면
   "다음 유닛만 누르면 된다"가 첫 코스에서 끊긴다. **E2를 E1보다 먼저 열지 않는다.**
2. **E2 → E3 → E4 → E5 순서.** 건너뛰지 않는다.
3. **E2 착수 전에 정할 것**: 없음 — 회화 4줄·유닛 20개·`notice` 규칙이 이미 이 라운드에서 확정됐다.
4. **E3 착수 전에 정할 것 두 가지**:
   - **ID 대역 재배정**(§8-2 — 9000 대역이 E2에서 찬다). senior-dev·`03 §7-2`.
   - **회화 6줄 상향이 실제로 적용되는 첫 코스**가 E3다. senior-dev의 수량 테스트가 코스별 하한 표를 들고 있어야 한다.
5. **E4~E5 착수 시 재검토**: 어원·접사(root/prefix/suffix)를 어휘 스텝 안 보조 정보로 둘지(§11-12에 남아 있는 미결). **별도 스텝으로 만들지 않는다**는 것은 그대로다.

---

## 10. 미결 / 사람이 정해야 할 것

> 아래 셋 다 **기본값이 있어 작업은 멈추지 않는다**(08 C-10 관행).

| # | 무엇을 정하나 | product-planner 기본값 |
|---|---|---|
| Q-E1 | **영어 확인 문제·음성을 언제 켜는가**(01 §10-6의 열린 항목 — "콘텐츠가 쌓이면"의 기준선) | **E1 15유닛이 다 찬 시점에 재검토.** 그전에는 켜지 않는다. 켤 때는 영어에서 먼저 만들지 않고 일본어와 **같은 틀로 함께** 붙인다(§11-9) |
| Q-E2 | **E1 15유닛이 다 찬 뒤 `/en/start`(자기 선택 5문장)를 문제 기반 진단으로 바꿀 것인가** | **바꾸지 않는다.** 콘텐츠가 더 쌓인 뒤 판단하고, 붙이더라도 자가 선택을 **대체하지 않고 보완**한다(§11-12) |
| Q-E3 | **유닛 3~5를 한 번에 낼 것인가, 유닛 하나씩 낼 것인가** | **세 개를 묶어 한 번에** 낸다. 5유닛 경계에서만 공개하므로 중간 상태를 배포하지 않는다(예외 E-3) |
